using less memory to count pairs 

+ matrix approach 

keep counts in a triangular matrix -> some pair 0 since not in the data

hash table -> use less data bc we dont store 0's ,also hash function must not have collision

given a transaction and hash table which stores just counts, losing the direct connection to the pair -> hash of the pair , there i store only the count not the pair that gerated it 

if i go there again i simply increment the count present there > we can have collision
always over estimation 

rearrange hashtable as bitmap -> where 1 if above some threshold 0 otherwise

to check if candidate transition are ok -> compute hashes if 0 infrequent and remove from checking 

another hashtabel that we hash only if both x and y are both frequent 

than for counts of candidate only if both hash are frequent 

build these 2 hash tables simultaneously 

scan less ?
partition dataset in chunks and for each compute the frequency and also change the minimum support accordingly (treshold) than union 

problems: 
1. missing something
2. more then needed 

the union my distort the actual count but the overall signal frequency remains
another pass to compute the total support and scale the counts by that ? 

---
toivonen algorithm

a small sample of the dataset is rapresentative 

