graph algorithm , vertex centric 

map reduce on graphs

splitting the data means breaning some links in the graph -> we need to share this

vertex centric -> tiny computation that runs on each node -> only knows infro of its direct neighbors

every node same computation 

every node local view not global optimal solution

only on undirected graphs , connector components are node connect with each other

select one node as the main -> name -> every node know him -> 
+ smallest id in the component
+ every other should know the smallest id -> forward 1 to all other nodes 

forward best knowledge to my neighbors and also collect it 

label propagation in map reduce

intially mapper knows about nodeid and assocaited neighborhood and best minimum known 
for each neighbor generate a message (reciepient(neighbor), minimum value)

second emit in map -> i need to send my neighborhood to the next iteration -> to miself in order to remember my neighborhood

reduce 1 per node -> has nwighbors and minimum recieved from them -> gets the minimum and emits himself and (minimum, Neighborhood)
output of reduce input of next map 

---
by building this we learn about new nodes > when i learn i "create" a connection between them even if not in the original graph 

either i know the smalles one or i know the smallest one 

send to the known minimum send all my knowledge -> the known nodes
to other the minmimum
each node merges node recieved -> new C(X)

the messages gets less and less the more we go towards the last iteration 

counting triangles
---

+ 2 neighbors connected by 1 edge
+ if 1 edge connecte 2 nodes and they have 1 neighborhood in common, number of common neighbors = number of triangles 

sorting edges -> to not recount the same triangle since i need to selec 1<2 -> its neighbors must be >=3 , instead 3 we need to choose a neighbor with larger id -> we will not chose previous nodes 

i cant split input graph bc i would need to check other partitions 

try to partition the output -> every reducer set of triangles -> if they overlap we may have data replication for the overlapped part 

every traingle to different reducer -> distinct id -> list of its nodes 
number of possible triangles R^3 -> not good idea

reducer work on sets of triangles instead of 1 -> new id -> hash of it 
hash -> like mod 2 -> the 3 coord i compute the hash for each and than ship to one reducer 

each reducer runs the sequential algoirthm and count triangles 

values are how we can initialize our triangles -> so the 2 nodes that start it bc hash loses info 

