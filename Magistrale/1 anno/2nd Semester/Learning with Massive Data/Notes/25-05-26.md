spark examples 

k means

cache -> first time you generate the output stored 

initial centroid -> take sample with repla -> maintains in the rdd the data -> action -> output not an rdd -> list 

2. for each point closest one -> create function for 1 point -> so that i can use it in map

assign will return as key cluster id , value is pointer to the point

unpersist -> removes broadcasted value ?? 

mappers -> geenrate cluster id and points -> after shuffling -> group by cluster id , we need to broadcast centroids 

kmeans++

diff initialization
init random points than compute distance between centroid and sample data point with prob 

all sequential steps 

parallel -> tries to make the loop cheaper -> run less iteration log(k) choices instead of k -> returns instead the k final centroids but get a set of candidates 

also we need to set oversampling factor between 1 an 2 

each candidate center is assigned a weight as the number of points near it , these weight used in weighted k-means to find k centroid

---

pagerank in spark 

parallelize the prod of M pi -> each rows to a reducer -> (key=row_id, value = content of row) -> if full of 0 store incoming of row i  

another mapper for (row i , pi) -> so that i can group by them and can do the product -> instead do broad cast since we would duplicte a lot of pi for each ri -> not efficent

pi needs to fit in main memory 

column focused 
-> extract columns from M -> 1 rdd each -> all node same outgoing nodes , ingoing instead they depend on other nodes 

join 2 rdd -> node + pi + list of outgoing edges 
map -> list of pairs -> key my neighbor -> my importance / outdeg ) -> for all neighbors 

firsm more efficent if dense 2nd better in generals 