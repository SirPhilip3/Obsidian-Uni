recommender systems

song raccomendation example -> easy to raccomend popular items

librarian approach -> single entity that raccomends something based on someone's intrest -> can be very accuratley

many experts -> michelin resturant reccomendation- > knows about restuarant but not me

friend raccomendation

quality measures : 
+ efficency of building the model > not really important since its not buld everyday
+ efficency in generating raccomendation -> important
+ serendipity -> soemthing novel ??
+ cold start problem -> what to do when we know nothing about a user or a new movie

song reccomender ?

we can trasform a user and songs in a list of features > linked togheter -> map to a vector 
content based reccomender system 

**users and songs must have the same features** -> user profile aggregate description of listened song

*vector space model* -> values -> number of words in the lexicon -> 1000s can be very sparse 
words normlization 

presence and non presence is ass

of word is there what info do we store -> $tf(t,x) \cdot idf(t)$

df of a term number of docs that contain that term -> 1/df

users -> is the average of the items that *positively* interacted with

basic choice of similarity > euclidean distance
compute similarity -> *cosine* of the two vector -> cosine similarity good for text
measures the angle between the two vectors -> 0 -> cos 1 -> maximum similarity
180° -> cos -1 

cos distance that doesnt take into account the lenght of the vector -> so same direction bu lenght different euclidean distance is that difference in lenght 

just calculate the distance between user and item and return top-k most similar items

QUALITY FEATURES
+ quilding > ez
+ generation > bad we need to calculate for every item 
+ serendipity -> small songs very similar to the one liked
+ cold start not that bad > as soon as 1 item we can push the raccomendation immediatly, user needs at least 1 song liked

info -> rating -> rating matrix -> *Users x Items* -> generally number of starts-> can be extracted from other infromation very sparse

instead of finding similar items find simialr users -> better serendipity
than use ratings from users to build reccomendation 

**User-based collaborative filtering** 

find similar users
**Pearson correlation** > large if 2 users increase / decrease similarly  -> normalize by its mean score

...

efficency :
+ very expensive , for every users we need to compare them with every other user -> precomputed offline 

and we just store topk similar 
everytime users generates a new rating -> users need to be changed

saerch ez -> if pre computed 

cold start -> new user doesnt interact witha naything and also items 

very sparse

in general userss >>> items 
can we be item based -> each item rapresented by a vector of size |U| -> where each is the rating

what is the rating that user gave to similar items 

person correlation but *not* normalized by item mean score -> we use user average rating 

