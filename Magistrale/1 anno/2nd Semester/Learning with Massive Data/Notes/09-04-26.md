gradient descent 

function with multi dim input -> we want to minimize the output
finding the best parameres of the model 

collaborative filtering 
given user and item -> predict rating user gives to that item 

loss function true value - our prediction -> dataset of user and item with theyr true rating 

learning is finding the paramers of the model -> ex for linear regression angle and intersect 

**UV decomposition** -> we can approximate the rating with dot product of two vectors -> user vector and item vector 

k -> how many feature for user and item 

dot product > agreement between user intrest and item instrest

ex user very intrested in an actor , the movie has that actor = big activation , no actor in  the movie small activation (small / big product)

algo chooses k -> from larger vector

automatically search for higher level topic in movie -> and each user how hes intersted to that topic 

topics automatically found 

$U$ matrix of $|U| \times k$ , instead $V$ : $k \times |V|$ , result of $U \cdot V = |U| \times |V|$
automatically find U and V such that theyr dot product minimized *RMSE*

in post processing we can see clusters in V and U 

k smaller than number of users and items

min the sqrt same as min the argoment of sqroot , also 1/|S| doesnt change the ranking > mean 

means gives us an average on how wrong we are on the prediction , removing this removes this

for null elements we skip it -> we may have no rating for some items from some users

min -> where der of the function is 0-> can be local min
der in respect to each paramer -> min for that parameter 

start from random paramerts -> in our case random U V matrices 

the true rating matrix is sparce -> there are gaps -> but the product is compleatly filled so we can predict unseen movies 

algo 
1. initialive U V randomly
2. repeat until convergence 
	1. for each u_rs set it such that partial derivative = 0 
	2. for each v_rs set it such that partial derivative = 0 

multiple passes since when i update something the prev will not be optimal anymore 

ther's analytical solution but very espensive in term of space and time for huge datasets 

finding the best k -> larger k will have error closer to 0 , -> overfitting to train data

smaller k = model needs to capture more patterns to compress information 

---
binary classification

model predicts the class of a vector 
linear classifier wa+b=0

margin 1 , -1 -> given the two support vector :
w x1 +b =1
w x2 +b = -1

maximize 1 / norm of the weights -> min norm of the weights

simplest model that gives the best decision boundary

no penalty if distance from center >= 1, otherwise 1-dist

---
gradient descents -> smaller iterative steps instead of tring to reach min immediatly 
more steps = more stable algo 

substract from gradient only learning rate times 

1/2 simplifies computation doesn change result > just reg

