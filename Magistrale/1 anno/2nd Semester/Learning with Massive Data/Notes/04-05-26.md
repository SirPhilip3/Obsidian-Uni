association rules mining 

correlation between events -> when set of event occurs also other occurs 

dataset = set of items bought 

we can have chains of events 

frequent itemset -> support aboove given threshold

we want that whenever X happends also Y happends -> confidence -> how oftern items in Y appeatr in transactions that contain X 

Intrest = confidence - support
how to find association rules with support >= s and confidence >= c

+ brute force 

list all possible association rules 
compute support and conf for each 
prune the one that are not above threshold

first focus on frequent itemset than focus on what rules can be generated from there
on the left all possible itmeset than on the right the remaning part 

1. find frequent itemset with support > thershold -> search space is exponential
2. generate rules 

second step easier 

k^n -> k level and n single items

the first levels are more probale to be more frequent 

if a lower level doesnt have enugh threshold all of its greater sets will also have lower support

pruning -> we can remove supersets of < threshold items 

*apriori* algo -> start at first level if something not freauent kill all supersets 

how to generate candidates -> we want the smallest number of candidate

+ bruteforce

compute all the possible combination of lenght k

combine with themself souch that they share k-2 prefix + do candidate pruning 

beer -> frequent 
Milk -> frequent
diaper -> frequent
bread -> frequent 
other not 

the frequen just mix with the others 
be -> mi
be -> dia
be -> br
Milk -> diaper
milk -> beer
diaper -> beer

--
spark
broadcast set of candidates to all worker 

small k -> very large number of cancdidates 

