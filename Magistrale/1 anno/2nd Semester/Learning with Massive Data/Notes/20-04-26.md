mapreduce 

distributed algorithms 

computing clusters 

Storage -> distributed file systems -> global namespaces but data can be wherever

hadoop implementation
+ master node keeps name of files + location = machine can be replicated
+ files stores in chunck servers -> divided into cuncks and stored in more machines -> we can read fast -> read in parallel
+ each chunk replicated

concurrent reads 
single writer 
writer can only append

type of files :
+ sequence files -> key value pairs
	+ write with append
	+ read by using next
+ map file -> sorted sequence + index to lookup by key 
	+ index subset of keys and theyr position in the data,sequence file 

two user op :
+ map -> takes key value pair and produces list of key value pair maybe of different types
+ reduce -> given 1 key and list of values , produces list of key value pairs
as many reduction call as the unique key generated by map 

if algo can reduced to this than parallelization op is possible

---
mappers and reducers processes 
one mapper 1 split of the input -> 1 chuck in the fs
shuffle phase -> id the distinct key and associate the value associated by it -> group by 
implemented by the system
reduce phase -> a reducer produce a portion of the output

mappers when they generate will write intermediate locally -> efficency reason 

reducer works only after all the mapper have finished 
different ouput for each reducer

master node 
+ keep status
+ keeps tracks of failures , recover them

if map fails -> map function is lost -> also all the data , worker resets to idle

how many map and reduce -> larger than nodes 
reducer see some completed jobs start copying data 

more reducer than machines > some idle that start working only after the reducer complete , immediatly , wasteful ..... 

combiners 

job of reducer can be done by the mapper already since it divides already the data by key 
before shipping -> start by reducing local data 
combiner -> does this

partition function -> which key to wich reducer generally reducer get various key not 1
changes the routing to balance the load across reducers 

backup tasks
same task can be done multiple times 

overcome slow workers -> which job finishes first wins 

mapreduce not for performance done for scalability and failure tolerant

shuffler weak point 