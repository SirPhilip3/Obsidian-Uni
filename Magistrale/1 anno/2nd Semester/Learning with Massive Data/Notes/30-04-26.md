map reduce all pairs similarity

algortihm for map-reduce

friend relationship 
find all the user pair of friends that are friend of a friend u -> x -> v .. ret u,v

for each user in the graph 
  for each neighbors of user 
      for each neighbor of that
         out the neighbors

1 iter of each for 1 thread 

in term of map-reduce

map phase -> part of the data to different reducers 
reducers di the actual computation 

starting and ending node same ? 
mapper input 1 pair

2 mapper 1 in charge of u-x and x-v -> both to the same reducer the cant talk to each other

somethin in common between -> x as a key -> if i put key x -> u-x and x -> x-v
value will be u-x and x-v
but mapper produces x -> u-x and u -> u-x bc we dont know maybe u will be in the middle u is in the second part

`for ... in edges` 

---
all pairs similarity 


find all paris with sim grether tha some threshold -> generally remove the ones close to 0.5 removes duplicates 

each row doc each col is a term -> score can be 1-0 , frequency or tf-idf
distance  = cosine -> if normalized by lenght just dot prod


reducer(key,values)
divide in 2 data -> 2 keys 

document by document matrix 
needs all the documents -> to compare the similarity between everyone else

map (iddoc, docitself) .> ship to both reducers
	emit(0-4, doc) -> reducer 1
	 emit(5-9, doc) -> reducer 2

duplicates the data ? 

not feasible for large dataset
divide more the data ?

best idea -
+ 2 docs that share at least 1 term than sim >0 
+ term as the key -> if they have same key -> than we can calculate sim

we duplicate doc a lot 

sort terms by decreasing frequency , for every col get max value store the fitional doc with all max terms 
first we do sim with fictional max and our if lower than threshold than d  cant have more similar doc

sim term by term -> either the score overcomes or thers one border term b(d) sc before we are below and after above , when doing sim against the max doc 
if 2 docs are similar the other doc must have common terms beyond the threshold 
search 1 term in common above that border , border comuted by mapper

avoiding to send copies for more freq terms

for each term produce sim useless, decision must be done before since reducers dont communicate

only the producer with largest key does the computation , sintersection of docs if key largest in the intersection i execute otherwise i die 

min could be smaller than the border no reducer will execute

