spark

spark tries to be membased 
like a pandas dataframe where computation

+ cluster manager -> manages worksers / jobs etcc , we are here 

sparkcontext api interface that we want to use 
some op need comm between worker nodes -> generally small data transfer between driver program 

executor > manage task locally 

resilient distributed dataset -> if something fails RDD is not broken -> doesnt stop the processes
our job build RDD and than define op that we want to do on that data

transfomration -> create new RDD from RDD -> accumulated in a data structure than when i need the it will run them -> given global view we can optimize theyr execution , forced when issue ACTION

ACTION -> operation that force to generate an output to be stored in a final repo

minimizing datatransfer -> same machine comp and storage

we can specify number of slices to parellalize 
more partitions than cpus -> better to have smaller task so that we can assign them 

**TRANSFORMATION**

map -> applyies function to every el of the rdd -> output new array , easy to be parallelized 

getting the result -> 1 action -> give me all the data in a new list 

flatmap -> flattens out output of mapper -> like  tuples to normal list 

filter -> takes where function is true 
sample -> random samp of data with or without replacement
distinct -> keeps distinct values 

union -> we can union of 2 rdd -> just concatenation not remove duoplicates
intersection 

**action**
+ count
+ count by values ->for every distinct value count occ 

take give few el from which partition you think its best not random not sorted -> to check for computation 
top takes largest elements

reduce aggregate elements on a function 2 input 1 out same type, op should be associative adn distributive since we dont know whic elemeten are taken 

*aggregate*

more specific in aggregation -> start the opr with an init value -> result can be of different type -> accumulator -> when sum with next number another accumulaotr > final accumulator -> sum from another process 

init + value = accumulator
accum + value = accum
accum + accum = accum

i need theses 3 functions 

average -> sum and number of elements -> should be 2 actions
1 aggreagtion that sum and counting at the same time 

accumulator tuple -> sum and count -> initial value 0 0 

foreach a function to all the element in rdd 

---
each element *key value* pair

identifiers -> zipwithindex -> each element add id -> 0 to n-1 -> small cost to sync workers 
zipwithuniqueids -> unique ids , not continuos we dont need to sync -> garanteed no collision 

database like operarions 

count by key > conts always first element
lookup -> collect given a key

---
caching 

transformation instrensting morethan once -> repeated action multiple times
reexecuting the transformation each time bc each time we compute it data gets deleted

spark has cache -> so that it access it instead of reexecutin it

---
sharing variables 

min and standard deviation needs to be global i want to be known between all partitions 
not only id to be accessible to everyon 

+ accumulators -> more for comm from workers to us -> like to monitor error -> add error

broadcast variables -> to send data from us to workers  -> only one copy per machine 
no update 

