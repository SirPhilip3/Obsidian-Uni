clustering 
K-means 

given a dataset automatically find groups 

cluster = set of objects similar from one another or hierarchi 

unsupervised -> no classes no labels -> we cant compare to true clustering 

types :
+ topic based 
+ biology

find outliers -> remove distant points 

quality measures 
how similiar objects inside clusters some draw backs 

similarity ? -> can depend on lots of things 

adapt to whotever kind of cluster 

---
clustering quality

sum of squared distances -> how cohesive 

mean of all the points = *center* 
measure the distance between center and any single point in the datased -> sum squared distances -> as low as possible = closer to one another

not an easy problem **NP-HARD** 

We want to *minimize* the **SSD** 

K-means
---
given number of centers

start with random centers -> given from the dataset 

until the SSD is > than some thershold or until  clusters keep changing or number of iter
assign data to nearest centeroid

after assigning all the points we update the centroids, where closest possible in the cluster

sensitive to starting point
converges to local minima

different sizes , not spherical is a problem
line equidistance from each center -> defines the class


elbow method -> test various k and find point where improvment is not large
in general larger value than actual number of clusters 

better have more clusters and each being more accurate 

K-means++
---

bad if center close to one another , conflict one another for the same set of points

better initialization -> spread across your dataset 
first always random -> next prefer points that are far ->compute all distances from centroid , distance is prob of being selected , depends on the smallest distance to the centroid , far away to the other centroids

P denom is just normalization, not the most far away bc normally they are noise

the expected arror is at **most** $8(\ln k +2)$ times the *optimal SSD* 

not expensive , linear in respect to the data , for large data bad

Mini batch K-means
---

even a smaller amount of ponit can give us the correct centroid -> than the classification is based on them

mini bach -> sample of the dataset -> every time we change it , if we dont do this centroid is based on samples > if we remove all the points from one cluster ? 

speed up convergence -> for each clusters store v -> keeps track how many time we update a centroid -> if we update it several times than we maybe closer to the actual solution

new center -> average between prev centroid + the single instance that we select (attract to them) 

decreasing learning rate -> initially we move more towards instances selected after that we move less 

convergence garanteed by learning rate -> goes to local minima

large samples -> we are doing kmeans, smaller we are approximating it

more clusters the more we need larger batch

k means and mini mach produce different output , if they provide comparable clustering of the same quality 

choose b that produce clustering as good as kmeans -> than we can compare performance 

CURE
---

cluster of arbitrary shape 

larger dataset > partition the dataset , cluster each partition and than merge them
arbitrary shape > hierarcy

1st step > random sampling

if we gave them the smallest size we can get the pest size of random sampling 

2nd step > m -> size of sample -> if its too large for memory divide in p groups , cluster them independitly -> from each group we want to find m/pq clusters where q -> average cluster size that we expect 

3rd step initially every point is a cluster -> put togheter points in closeness > util we get number of clusters that we want 

we store only c rapresentative points per cluster 

minimum distance between rapresentative is the likeness between clusters
rapresentative well scattered 

first is the furthest away from the center , reaminig the furthest from previously chosen point + some points sselected experiemntally 

this are then shrunk towards the center of the cluster 
1. to tune how much we want to have non spehrical > full at the center = kmenas, more distance = more general shapes 
2. effect outlier points the most affected by the shrinking , the most far away shrunk more

reduces false merging 

4th step merge all the clusters togheter -> same way as the single partition until k cllusters  

outlier removal : 1/3 of the data points = number of cluster we remove cluster whith very low points
and than at the end when we are closer to k clusters

5th
scan every point and assign to the closest rapresentiitve point

cure expensive m^2 log m
we need to have m-k merge operations -> we need to compute distances each time with other clusters 

we use min -heap to store disatnce

evaluating computationa cost -> large dataset
quality -> take classification dataset laready trianed -> run clustering and compare to that 