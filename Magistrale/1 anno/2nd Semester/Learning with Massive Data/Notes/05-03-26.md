page rank + numpy 

every page starts with some importance 

convergence -> the importance doesnt change 

problems on the network
+ sink / dead ends -> recievs importance but its importance will be lost 
+ cycles -> the importance will not be stabilize -> from large to low etcc

remove dead ends and cycles 
remoing dead ends = add fake edges to the dead ends to every other possible nodes on the network

also for cycles -> 80% importance to normal edges 20% for the added ones 

$$\pi^{t+1} = \beta M\pi^{t} + (1-\beta) 1/n

\quad\quad \textsf{with}\quad\quad

M[i,j] =  \begin{cases} \frac{ 1 }{o(j)} & \textsf{if}\ j\rightarrow i \\

1/n & \textsf{if}\ o(j)=0 \\

0  & \textsf{otherwise}

\end{cases}

$$
M is the matrix that rapresent the graph
M\[i,j] links from i to j 
1/n if o(j) = 0 -> spread the importance of the the dead end to evryone else
(1-beta)1/n -> the 20% for the unoriginal edges 

$\pi^t$ is the page rank 

we stop when $\pi^{t+1} = \pi^t$ 

---
numpy

integer in py is an object -> infor for gb, integer is pointer to that object

list of integer is not space local -> chasing pointers
special temporal locality not satisfied

numpyarray -> c array 

strides lenght of rows and cols -> to jump

asarray -> input from list and creates a narray 
if input already an array it becomes a pointer to that data -> views on the original

we can specify datatype

half precision -> if we donts stay in memory , or in cache locality
