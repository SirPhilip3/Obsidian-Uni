Domande Learning

+ SimHash not correct to use a number of bands b=2 when searchin for docs whose signaure differ by at most k=2 bits 
> undetected bc of no match on both bands

+ Describe SimHash
>single bit signature , multiple bits , cosine similarity estimation , banded-approach

+ Describe map reduce algorithm for counting the number of triangles on a graph
>output partitioning approach, buckets , map function , reduce function

+ Describe collaborative filtering item-based raccomendere system
> rating matrix, user profile, item profile, sim function, pros/cons

+ Describe methodology for finding the optimal number of cluster in k-means
> repeat for many k values , elbow method

+ Describe K-means
> initial centroid, assignment step, centroid update, termination criteria

+ Given a set of user-movie ratings, how would you evaluate the performance of a recommender system?
> split train vs. evaluation (take time into consideration), decide on a measure

+ Explain why is it difficult to evaluate clustering. Present a commonly used error measure
> no ground truth, SSE, apparently not found in us

+ Describe a Content-Based recommender System
> item representation, user representation, similarity, NN search

+ Describe the Product Quantization method
> Encoding, Decoding, distance approximation

+ Can you discuss an initialization method for k-means different from random sampling
> kmeans++: first center is random, then each subsequent center is chosen with probability proportional to the square of the distance from the closest existing center.

+ How difficult is to evaluate a clustering? What are the main criteria?
> no ground truth, inter vs. intra class dissimilarity / similarity

+ Describe the Hierarchical K-Means (HKM) algorithm
> Indexing, Searching algorithms

+ Provide a description of the Approximate Nearest Neighbor Search Problem
> problem statement formula

+ Describe the PageRank Algorithm
> Adjacency Matrix, convergence criteria, teleportation, dead ends removal, impact of beta

+ Hierarchical Navigable Small World method (HNSW)
> Layer Assignment, Traversal, Insertion, Search

+ Inverted File Index method (IVFADC)
> Indexing, Search algorithms

+ mini-batch Kmeans clustering algorithm
> batch, find (find min), update (new min), learning rate
