third assignment

find near duplicates

*simhash*

find points such that angle very small between them

prob that a random plane splits 2 poinst -> theta \ pi
theat = sim 

signature of the points -> use hyperplane 
rapresent with 1 bit -> 1 if above hyperplane 0 otherwise 

single bit signature 
simhash -> concat of m such signatures 

expected number of different bits between 2 sign -> m theta/ pi 

count different bits -> can estimate the angle between 2 points and so the cosine similarity 
typically number of m small << dimensions 

still we need to compute simhash for all the couples

draw random hyperpale 1 time -> build random matrix -> every row a threshold , doct product with docs -> sign of this positive  = above , negative = lower

use only the signature to find similar docs

docs sc signature different by at most k bits 

+ brute force ->compare all pairs 
+ banded -> divide simashd into b bands of equal size
	+ find docs sharing at least one identical band in the same position , only in small grups brute froce 
	+ for each pair compute exact differences 

each band independently 

spark -> group by to confront bands 

