Approximate nearest neighbor search 

abstract rapresentation of data -> vectror embeddings in vector db 
query -> transform it in vector > search in vectror db -> similarity

Dimensions = 100s

nearest neighbor search 

$n*$ -> index of doc -> $x_{n*}$ > the actual document 

linear search -> expensive 

---
approximate nn -> within a factor (1 +epsilon) of error

compact rapresentation of the dataset -> also related to speed

*Product Quantization* -> partition original D vector in M partitions , compressed independently 
to compress i look into the whole datased for each batch 

replace with limited vocabulary -> k elements -> key  vectors
replace input with this with as small as possible error between them

![[Drawing 2026-03-30 12.35.02.excalidraw.png]]
%%[[Drawing 2026-03-30 12.35.02.excalidraw.md|🖋 Edit in Excalidraw]]%%

**K-means** on the datased and substitute the points with theyr centroid , more clusters = better approx 

by looking at the centroid distance we can remove lot's of not close data 

store id of the centroid -> table where we reference them 

PQ code

cost : sub encoder O(KD/M) , where D/M -> size of cenrtoid , K = numbers of centroid sotred 
encoding time O(DK)

encoding *error* 

we justaxpose the original centroid -> compare de distance to the orignal one this approximate the error that we are doing -> minimizin this error

true pints only inside that error radius

distance computation -> compare distance without using the inverse , using the id 

Asymmetric Distance Computation 

given query compute distances against the centroids not the data -> store on a table -> scanning the data is simply looking into the table 

distance are additive -> M lookup

asymmetric bc we compute the distance not aginst the actual data but approximated centroid 

cost-> precomp fast

we still need to scan the whole dataset -> slow

---
find the closest cluster and than compute the distances only inside the cluster , can be done hierarchical 

link each data to the centroid with postings list 

insead of storing the full doc -> residual in respect to the centroid -> more efficent compression if we need to focus on just that region

typical clsuters $\sqrt{N}$ -> each clusters in general $\sqrt{N}$ points
reduces number of distance computation

---
focusing on quality -> more memory not feasable for large dataset

store dataset as graph allow computing distances only between neighbors in the graph

move towards closer and closer neighbors, edges are fundamental, still approximate 
node that is defined as aentry point

must not be fully connected otherwise we just compute all distances

graph layers -> higher more sparse lowest all nodes 
more sparse also more distance -> so we do initally long jumps than refine with closer nodes at the end 

building graph -> *INSERTION*

1st > what layer do we put it ? -> every node in 1 layer and all the below one 
choosing with log unuiform dist

M hyp parameter -> how many neighbors 

**traversal**
....
**insertion** 
....

search 
---

from top to 1 -> greedy search
for layer 0 -> beam search -> pick set of candidates 

pq ->very fast data structures 
graph -> cost in terms of performance slower data structures 

graph -> easy to ad new nodes -> pq we should rerun kmeans after some time

use graph to compte the first layer and use it as coarse quantizer for PQ 
