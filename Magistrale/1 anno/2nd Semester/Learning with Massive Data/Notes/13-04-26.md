ranking in search

sorted based on qual of result on a given query -> user percieved quality 

dataset of labeled examples 

rapprsentation function -> given docs / image etcc to vector 

class of ranking functions

training loss function to calc the loss and better the ranking function 
eval metric -> how to decide which model is better 

trec 2019 -> query -> some text with relative "qual" -> 1-5 label

implicit : show them 10 result and the clicked will be the label -> 1st etcc but since the trust the system prob will always click on the first one 

noise -> ranking dependes on the users and users are very different lot's of external factor 

---
rapresentation 

for each word how much is important for that docs , another signal may be pagerank ,number of clicks etcc

----
rank

+ linear regression

ranking metrics -> 
+ mse 

classification loss 
loss 0 if ranked correctly -> will never be 0 , close to 0 is very close to the solution 
1 otherwise 

not good since we give 0 to also close rank , wrong rank position doesnt mean bad rank

kendall tau -> distance between 2 sorted list -> compares every pair in the one produced and the real -> if agreement order -> +1 corr -1 if ranking "inverted"

but same between switching 1st with 2nd and 1st with last , in real words worst the second scambio 

discounted comulative gain @k -> k top k result

the higher put the best docs best gain 

gain -> depedns on label 
discount-> dep on the rank 

lower rankning -> user less likely to watch the docs

rank 1 = discount 1
rank 3 = discount 1/2 

ndcg -> norm between 01 -> der not defined or 0
no gradient descent -> proxy loss function

loss dec if the docs get a larger score 

maximize the margin

---
searchin for a ranking function -> gradient descent in the function space 

F1 = F0 - gradient

der of loss in respect to the function
gradient of the cost in respect to our grading function is the loss-
function that fill the error 

never defined a loss function -> gradient witout it

tabular data -> gradient boosting 

decision tables -> same test per level -> 1 single computation for level 
test time ->test same for every instance 

just sotre the outcome of the test as bitvecotr > with leaf wewant to go 0 ,1 left right instead 

-> encoding of outcome -> position to look on the leaf -> forces algo to find general patterns 