graphs 

nn on graphs

neighbourhood similar to images but here there is no ordering and they can vary / different type of neighborhood

as cnn we know also the neighborhood -> we want to find similar convol per graph , same response when they are isomorphic , 1:1 mapping of nodes

permu invariance and equivariance

graph no edges > each node set of features 
stacking them togheter -> implicit ordering -> we need to have function agnostics to order

summation , average , min , max 

permutation matrix -> reorders the matrix 

permutation invariance f(PX)=f(X) -> if holdf function is perm invariant
P = perm matrix 

*Deep Sets* 

$\psi$ -> feature transformation 
sum > of feat trans -> resulting vector = whole graph

no edges , no relationships etcc 

*equivariance* 
f(PX) = Pf(X)

---
with edges - unweighted adj matrix -> A

just shuffle properly also the col and rows of the adjacency matrix 
PAP^T 

PA -> trans rows 
AP^T -> trans cols

neighborhood -> set of the feature of the neighborhood -> X_N_i
wrap of node itself and the neirby nodes -> new rapresentation 

latent rapp lets us do :
+ node class
+ graph class
+ link pred 

aggregation of neighbors > diffusion 

gnn
1. conv
2. attention
3. message passing

conv gnn > given a node info from neighboring node

