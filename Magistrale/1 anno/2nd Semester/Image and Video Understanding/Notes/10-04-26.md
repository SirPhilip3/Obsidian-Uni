attention -> some interpretability 
dot product of 2 vectors and some normalization 

transformers

recurrent models issues 

are unrolled left to right
rnn > to rapresent we need to pass the whole sequence trough the lml

more distant element cause problems

non parallelizable 

can't train on large dataset

attention as soft lookup 

instead of key -> strenght of connection between query and key -> how similar they are , val -> sum of all values weighted by score

*self-attention*

each word -> 3 el key val query,

query which other word are relevant in respect to other words

output -> new rapresentation for the words 
q k v -> shared between all words

op parallelizable for each query after thransforming each word input in the 3 embeddings q k v 

*no real ordering* everything connected to everything 

add positional encoding -> agumenting rapresentation with when el arrive in out seq 
add the vector pi for each v q k 

????
sinusoid > ortogonal rapresentation between 2 > 0 
fully deterministic 

absolute positioning 

---
learnable params

encoding position ohe encoded to MLP > return d dim encoding 

no non linearity in self attatntion 
several layer of self attention , between each self attention to feed forward nn > MLP

---
we can't look into the future while traning -> cheating -> normlaly does bc fully connected 

masking -> dont want connection -> 0 the corr score 
\- inf -> in soft max becomes 0

O(n^2) complexity

Transformer
---

encoder decoder model

inputs -> + add positional encodings
than self attention 

K Q V -> learned 

just self attention but matrix

scores -> XQ x (XK)^T  ->  n x n
normalized -> softmaxt (scores) -> n x n 
rapresentaion -> noramlized x XV  -> n x n xx n x  q -> n x q 

Q can be in different dimensionalities 

*multi head* -> more level of relationship -> Q K V  -> divided into h subparts indipendent from each other

each head learns different links like -> entiites or syntactically relevant 

scaled dot product -> score divided by $\sqrt{d/h}$ d = dimensions , h = n heads -> to avoid that some head becomes too important -> scale by theyr dimensionality

*add and norm* 

residual connection -> smoother gradient 
on the output we are adding the input -> forcing the layer to learn just the difference between the two 

loss changes much less 

*normalization* -> rem the mean and diving by the std -> centered in 0 and std of 1
than we rescale 

stabilize the trainig

N layer

how many head hyper params 
asses on val dataset

