image classification 

google net 

inception -> network inside of a nework
each layer recieves signal and 4 filters in parallel -> ouput concatenated and fed to next 

before each convolution -> 1x1 conv to remove some complexity reduce dimensionality 

stacked  multiple inception layer -> each filter capturing different caracteristics of the image eachdifferent receptive field 

initial part of conv pooling until some dimension and than multiple inceptions
at the end a classifier ouput 

in the middle additional classification output -> helps gradient flow 

---
RESNET 

more layer = better res  > not cause overfitting but trainig much harder to train 
resnet -> just learn what changes from one layer to another not the full layers

stack multiple residual connection inside 2 conv  layer 

bottleneck layer -> 1x1 conv to reduce dim 

initially instead of random weights > random / dimensionality -> normalized weights 

neural architecture search -> auomatically find the size and structure of the nn 

---
transfer learning

leverage other models 

ex in laexnet initally recognizes the geometric shapes -> we can use that for other specific tasks

download pretrained model -> freeze all the weights except to FC layer -> last layer -> for small dataset

bigger remove the last part -> last fc layers -> more  data more deep we can go -> lower learning rate 1/10 -> we want to modificate less -> rule of thumb

semantic segmentation
---

each pixel a label 

input pixel to MLP to output 10 classes 

inefficent keeping a neighbourood nad than give each to a cnn and classify only middle as that class 
encode the whole image with a conv nn and than semantic segmentation 

given image series of convolution with size same as image 

first down sampling (reducing dim) than up sampling to same initial dimension 

upsampling -> 
unpooling -> given activation expands with same values in the pixels , or with 0 values

bilinear interpolation > distance between the corners and equl distance

bicubic interpolation 

max unpooling -> remembering where the max was in the original pooling we put the new values back there 

learning unpooling from2x2 to 4x4
transpose convolution -> overlap = sum > we can just learn a 2x2 kernel stride 1 -> overlap -> only +1 dim insead stride 2 = no overlap duble size
1d transposed -> from 4 to 6 dim 

finer details by combining more conv pooling 

encoder decoder arch as encoder is the downsacling part , while the decoder is the updacaling , + we keep pooling indices 

u net -> layer -> /2 size but double the feature response 
contrary for the extraction