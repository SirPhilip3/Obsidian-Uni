message passing gnn 

nodes exchange messages 
function that recieves embedding of nodes -> takes 2 out 1

no attention / weights -> all learned by function
worse scaling > compute all messages -> more general framework

summarized recieved information we sum up to the current stae nad return next node state

update function -> update emebedding of the node based on the collected messages 

t times -> hyperparameter

M U -> learnable nn

readout -> emebdding for the whole graphs -> if we want to perfrom operation on the whole graph 

each t further and further away collection of info

also time sequence can be transformed into graph

---
<img src="https://cdn.7tv.app/emote/01HPMK442R000D7CDG3FWMSHWB/1x.webp" alt="LOCKIN" title=":LOCKIN:" style="display: inline-block;height:5em;vertical-align:middle" onerror="this.src=this.src.replace('cdn.7tv.app/emote','_7tv-emotes-cache').replace('/1x.webp','.webp')">




Image classification comp vision

normalization layer
batch normalization -> recenter the mean and scale by the varaince -> next layer same scale 
we are not using the whole data

for each dim -> mean and std -> 0 mena 1 std -> restrictive -> we want something more near original data

learnable rescaling paramesters after the normalization :: y \* normdata + b

removes noise -> larger learning rates > faster convergance

-3 < 3 > most likely noise 

batch norm to compute mean and std consider per channel

instance norm -> mean std per elemente per layer

group norm

1x1 convolution -> compress rappresentation to smaller dimensions by keeping spacial dimesions

---
examples

data augm -> to rebalance classes 

stacking 3x3 layers -> same receptive field as 7x7 but we need to learn less parameters 
deeper nn while maintaing low number of params -> more powerful emebeddings 

most prameters on the fc -> low mem -> need for weights -> params used to compute the 4096 vals

vgg also used for other shit , ensamble at the end -> multiple decisor and than merge output 