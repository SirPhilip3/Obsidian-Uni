---
~
---
video obj tracking start on 1 frame search for same object in next frame 

detector fails -> occlusions , viewpoint etcc , background clutter
also obj may have different phisical model of motion -> trajectory prediction 

object may disappear from some frames -> long term correspondance 

appearance how the target looks like 
trajectory prediction 

at time t crop tracked obj , at t+1 crop same region -> conv nn to both tahn mlp regressing location of the object from one frame to another 

if similar than same obj is there 

cycle consistency ->< self supervised way no labeling -> strt from sequence -> training offline having all the sequence 

start from the start and predict end - do same in reverse -> perfect tracking start == end

---
each detection of the object + feature -> this seq to lstm -> each location of the object 
lstm for temporal

heatmap -> where obj could be in the image-> fed to lstm motion as recurrent model

multiple obj tracking of the same type 

online tracking -> only 1 frame at a time (2 by keeping the prev) 

offline tracking we can more easily recove from drifiting

1. initialize with detector
2. predict next position-> motion model opticalflow
3. match prediction with detection -> appearence model same person = same dress etcc

kalman filter ->2nd -> uses law of motion
tranformer architecture > assume constant velocity 

object proposal comes from the object detect at t-1 -> reuses the weight for obj detection

search for obj near the proposed -> without motion model 

we can train on still images of same objects

cons -> no identity -> classe of objs
bad on occlusion and fast , large motions 

reidentification and motion model fixes 

---
appearance model 

as retrieval models -> ids in a db , meauser closeness between images = cosine similarity -> vec the image with same size  

learn distances -> learn sim metric 

---
use siamese network -> create theyr encoding and confront them , use mse between objects as loss function -> different person = larger hinge loss -> pushes different images far away 
*contrastive loss*

pos = same person
neg = diff person

y = label -> if pairs are same person = 1 otherwise 0 
if we have positive pair we minimize distance otherwise maximize 


---
triplpet loss ->anchor + positive + neg , anchor distance with positive is smaller , higher with negative 
also ranking

harder cases are more important -> samples that have similar disatances between anchors are hard cases 

balance betwee easy and hard probems 