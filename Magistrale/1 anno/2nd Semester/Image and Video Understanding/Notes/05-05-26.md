transformers 

instead of decoder object queries -> each at the beginning nothing -> decoder refine -> ourput potential objects 

cnn -> as backbone 
some object queries mybe no object -> limited for budget -> even if we dont know how many 

tranformer -> from image space to bbox 

---
same as per llm but no maksing since we dont care about looking at other bboxes 

we can investigate the attention on the encoder part 
decoder geerally more attention on the limit of the object since we need to give bbox

instance segmentation
---

detection + segmentation 

add additional task to fast rcc + mask prediction at the end on top of the region of interst -> segmentaion nn -> different losses

mask predicion > 0,1 mask location already provided

now mutiple object of same class > multiple masks

**transformers**

same
refine attention masks to real masks

evaluation
---

positive match -> jaccard > 0.5

per class evaluation better to do bc for some classes it may perform good or bad 

ranking based on confiedence

area under the recall precision curve to confront various models 

vision and language
---

image captioning 

image encoder and txt decoder 

textual decoder for the image -> recurrent nn as input image vector 
problem is with recurrent nn -> after some time the weight from the image is fully gone 

size of context vector -> not all info can be kept , also we lose spatiality 

attention 

instead of feeding cv -> attention model 

splitting feature map in small cunck -> each of those to generate the sentence 

attention -> alignment matrix for each patch at each time of the generation 
than the generated word to create / modify alignet matrix 

transformers
---

each of partial feature trandformer encoder > produces context vector -> given to decoder

we can remove the partial feature just give image patches to encoder


CLIP
---
rapresentation alignment -> object in different modalities -> find common latent spaces 

do not rely on label -> supervision from textual description -> we dont need to train model on specific task just ask it 

same feature vector for the image and the text 

dot product = 1 -> 2 vetctor equal -> minimize the dot product between the two encoding of image and text taken from theyr encoding 

main diagonal the pairs that are in correspondence -> matches between text and images -> on other the things that should have diffent embeddings 

maximize main diagonal and minimize the other parts 

similar emebedding with similar data -> totaly independednt from the label 

