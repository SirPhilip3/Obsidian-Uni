graph neural netwoeks 

1 bounding box = 1 node  

tracking = 2 node are connectec with each other 
no edges between node in the same part of the graph -> box in same frame 

learn relationship between bounding boxes we can also have features on edges 

edge classification -> 1 = same obj , 0 = different obj

node embeddings -> combining neighbouring nodes 

new edge rapresenation -> mlp given previous edge emebddings + features etcc 

h_i , h_j = emebdding of nodes h_(i,j) embedding of edge

candidate matches between frames dont care about inside the frame 
classify each edge as good match and bad

we can match also along longer distances

for each node cnn -> for the box -> shared weights also for *MLP*  

MLP keep not only appearache but position , time , size 

SUSHI
---

2 layer association -> short term lack robustness for longer  

long term -> keeps more but size explodes 

sushi -> offlien tracking hiherarchical -> long tracking 

hierarchical graphs 
1st -> detection -> from one layer to another 
2nd -> longer tracking time 

each later independent but same arch each layer shares weight of prev ones

eval chriteria
---

mota 

genAI
---

manifold <img src="https://cdn.7tv.app/emote/01JZK2FF5954BFNT8ZVJDJE257/1x.webp" alt="catPiss" title=":catPiss:" style="display: inline-block;height:1.5em;vertical-align:middle" onerror="this.src=this.src.replace('cdn.7tv.app/emote','_7tv-emotes-cache').replace('/1x.webp','.webp')"> ->space that contains images of some geomshape 

autoencoders learn to map images in lower dim space with bottleneck 

most images are noise 

remove encoding > input in the latent -> rand numbers  

image to image -> we can do like colorization

variational autoencoder 
shrinking int mean and std -> each dim = gaussian

sample value near mean -> so actual images used in the train -> when decode we have the orignal -> train dt 

clean up latent space -> doing this give random get image confront with original -> error etcc 

GAN

generator -> gen fake image to fool discriminator
discriminator -> distinguish between real and fake 

*Diffusion models*

map rand images into the manifold -> noise to images 

1st -> map to noise than from noise to img
to noise -> controlled 
start from noise remove it until image 

conditioning -> label associated to each point 

matching description 

