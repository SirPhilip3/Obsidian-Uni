transformer based semantic segmentation 

fcn are unable to capture long range relationship also receptieve fild maybe too small for some objects 

decoder after passing in the transformer >
1. progressive upsampling conv t oexpanf 
2. multi layer features -> reshape feature from each intermediate rappresentation of the image -> than add them togheter 

---
intersection over union -> how close prediction to groundthruth -> jaccard 
pixel accuracy -> ratio of pixel classified correctly 

object detection 
---

object detect + classification + give prompt to detect

template matching pass it and check for match -> only on good cases -> size etcc 
also template need to be provided 

learn characteristics of object -> slow training but fast detection 

provide faces and fas computation of features
boosting for feature selection -> train small models and than ensamble them 

integral image > sum of all the pixel between two coords -> comulative sum  

compute feature of a part of an image by removing and adding edges of the part 

label images with the given features and train an classifier 
ADAboost -> weak classifier -> for wrong samples higher weight for that sample relearn another classifier on that 

hystogram of oriented gradients 

given gradeint image -> create a rapresenter for that 
histogram contains feature descriptor 

detecting a person by composing different body part

non maximum suppression 
---

we can have many boxes that possibly contain the element -> suppress some keep the best boxes 
do jaccard on all bounding boxes -> keep the maximum

2 task :
+ classification
+ regression

multitask loss 

