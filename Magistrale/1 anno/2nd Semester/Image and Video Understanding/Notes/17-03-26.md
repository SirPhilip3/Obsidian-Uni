binary cross for class/ mean square for recession

set of par that minmize the loss

f(x, W) ->predicted
y -> expected 

gradient descent

analytic gradient -> symbolic formulation etcc ->
numerical = limit 

*computational graph* -> break down math function in main components, rapresent the function as a graph

to compute all partial derivative -> we ould like to have partial der in respect to each variable 

build comp graph , collect derivative going back from the back

inside of each neuron only on some part of the derivative not all 

stocastic gradient descent 

random sample super noisy or mini baches ->random sample of k el -> weighted gradient smooth out noise

regularization -> we dont to make the network mimick the orinial dataset ?? -> to allowe generalization

regualrization -> add more data , data augmentaion -> train on simulated data

dropout -> avoid having part of the network too specialized 

early stopping -> elbow method 

convolution

semantic segmentation -> each pixel 1 class -> 2 cats -> same cat

exploit local structure

weight of the filter are learned
number of channel should match between image and filter

computnig the function where w -> of the filter, x are the pixel

output -> activation map -> number of dimension = number of filter
activation = 1 matrix per filter -> a response that it gives us 

next layer -> depth = n of filter

stride = amount of pixel that we are skipping when moving the filter
fractional -> we are reading in between pixel > we could get out of bound location -> problems -> we cant rapresent it -> find right balance 

