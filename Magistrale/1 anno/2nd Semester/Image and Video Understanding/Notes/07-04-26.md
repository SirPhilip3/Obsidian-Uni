rnn 

recurrent nn backpropagation trough time 
training -> before building model we need to know the whole sequence 
at each time we have a prediction and we can compute loass , the overall loss is the sum of all losses 

prediction depends on previous one -> longher seq = larger gradient that we have to sum up 

using chain rule -> product of all the weight of each step -> weight lower than 1 > goes to 0 , larger than 1 to infinite

long term dependencies 

theta -> shared parameters 
gradient clipping -> constrain the gradient -> if norm grater than clipping value we normalize the gradient by the norm and rescaled by constant c

c = hyperparameter

we could truncate at some point of the sequence the backpropagation -> only if task allows it 
keep the last hidden layer than redo for the next section 

smaller weights = vanishing gradient 

also the initial token is less and less present in the final rnn , in genreal it consider only the last moments of the sequence

rnn as language model 

autoregression -> output as input for next 
language model forcasting next word -> o

how the training works -> given a text we know exactly what is the next one , predict test against the words etcc , no label

**long short term memory network** -> solve the problem of before -> good for small amount of data

cell state -> long term memory what we want to maintan from the start
short term -> only the previous -> used to drive them to the long term part or short therm

3 gates : 
+ forget gate : how much of the long term should be remembered
+ input gate : how much of the input data will be put inside the long term mem
+ ouptut gate : updates the short term

forget gate -> network recieves input + short term -> predicting a vector -> each comp number betwen 0 1 -> rescaling closer to 1 remain , closer to 0 disappear  -> no amplification in this stage 

input gate -> 
+ from input + prev short term 
+ 1 does the forget gate operation
+ 1 does hyperbolic tangent \[-1,1] -> sign agree > increasing some component , sign disagree > reducing that component 

ouput gate -> 
+ considerning long term actual input 
+ revese of forget but the long term passed to hyperb tangent 

conv LSTM in computer vision 

insteead of text a frame -> instead of small mlb for sigma -> we use a small CNN gates are the same 

also for sequence of objects -> ex varaible number of leaf -> leaf as part of a sequence 

*sequence 2 sequence* -> transform une sequence to another -> translation 

encoder decoder architecture, input maybe different lenght to output
2 recurrent model togheter -> output as 1st input the second

encoder -> compress rapresentation of the whole sentence into a single vector -> decoder thakes the vectro and exploding it 

context vector -> we can lose the context of the question if we override the input vector 

size of the vector -> if context vector not big enugh we cant rapresent the whole "sentence" -> how much we can rapresent 

chosing next word -> maximum , or beam search -> explore multiple branches in the generation 

training encoder is effected by the decoder -> decode loss per word -> than bakcpropagate the gradient also to the encoder 

**addittive attention**

in the decoding we lose the encoder ouput ->bottleneck problem -> phrase into small vector 
we dont need to compress the whole seq we just need to access the memory of the encoder

attention matrix 

all encoder append to all decoders element 

generate context vector per time step for the decoder -> weight to each element of the encoder + current state of the encoder , part not relevant = 0 weight 

attention mechanism + sigmod gives score to each encoder state + emebdding from the decoder 
context vector for each moment -> weighted sum for the embedding and the score given by attention mechanism 

which word more relevante in the specific stage , word of the decoder 

emebdding of encoder fixed 

ex for translation attention scores "maps" trnaslation between words -> learned from data 

no more bottleneck and vanishing gradient