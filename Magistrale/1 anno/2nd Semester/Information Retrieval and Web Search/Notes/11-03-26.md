proximity / partial match  

phrase query > search exact phrase -> and in exact order, we can't use inverted index for this type of search 

we also need to record the position of the word within the document (positional index) can be lossless

biword indexes specific to process phrase query

alternative tokenizer where consecutive pair terms become a phrase

this works for 2 words phrases , for multiple words ?? -> we can break the query in 2 words phrases linked with and -> than use biindex --> result is a **superset** bc it will contain also docs than have all the biword but disconnected 

longer search = more false positive
index blows up 

positional indexes -> phrase search + proximity query 

```
<term, n(number of docs containing term);
	docID1 : pos1,pos2,...
	..
	docIDn : pos1,pos2,...
>
```

max pos = leng - 1 of the document

also the position are sorted 

to match phrase we merge the two list -> the gap between two letters must be of 1 
```
to : 
docID 4 : 16, 190
be :
docID 4 : 17, 191
```

4 is a possible document

algo 

indices can be compressed, instead of storing the identifier we store the gap between them 
position index 2,4x time larger than non positional , 30-50% of the volum of the original text -> this depends on the density of the language > more repeating langue = more compressable

combiantion schemes 

for common phrases inefficent -> we can use also with biword index 

---
structured data easier to query , unstructured data is what we find on documents

semi structured data -> in almost every text -> thers a title some pointed list etcc

elastic search -> structured combinaed with unstructured data 

Document ingestion
---

read the documents and build the index 

documed must be tokenized -> normalized -> index building

document can include text from different languages , document can be complicatext mail + a pdf are 2 different docs or 1

*tokens* 

given a document -> way to produce -> split the text in words by spaces and removing separators 
each token candidadte of becoming a term -> after some normalization 

normalization can lose information "finland's" if normalized to finland we lose the ability to do exact match on it 

for numbers how ? 

also languages impact this tokenazation 

