TAAT Daat

term at a time taat

Doc scores docs are computed one “query term” at a time  
(we’ll also discuss Fast scoring to optimize TAAT)  
+ We need to accumulate the per-term TF-IDF weights in per-doc accumulators

DAAT = “Document At A Time”  
+ Compute the total score for each doc (regarding all query terms) before proceeding to the next doc

efficent cosing ranking -> 
+ calculating each cosine efficently
+ choosing K largest cosine efficently , ideally without computing all N cosines

efficent *approximate* sol for low dim and dense vector -> our prob il high dimensional

taat not the best for mem managment , since we need to jump a lot in memory to add the accumulators in non contiguos places in memory

long list to be sorted -> we need to compute all the terms before rank ordering 

heap sort better -> stop sorting up to top k 

*daat* document at a time binary min heap of k elements -> when new better score update it 

best case -> top k doesnt change too much after first steps

---
WAND scoring

remoe all docs which cosine are garanteed to be below some threshold without computing the corresponding accumulator

at the end scorese only for un-pruned docs

we still need to do some first level eval 

for each posting list maintain an upper bound -> idf _ t * M -> M = max w_t,d

threshold = min score of the heap , when d enters the min heap thershold grows

pivoting
sorting by docid 

---
block max 

