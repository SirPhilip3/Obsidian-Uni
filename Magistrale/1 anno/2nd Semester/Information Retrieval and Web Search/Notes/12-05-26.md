web crawling 

fetch and parse page to identify other urls to be extracted to build a graph of links 
starting from seeder pages 

complications : 
+ malicious pages
	+ spam
	+ spider traps to make it stuck 
+ how deep into URL hierarchy should we go 
+ should be polite not too many requests
	+ follow robotstxt
+ not crawl site mirrors or duplicate pages 

should be distributed, should fetch high importance pages first , also update already fetched pages 

some website must refresh constantly

if url not complete replication add otherwise no 
for each extracted url check if satisfy the robotd etcc rules ->if in the frontier or alredy visitet not put in queue

doc fingerprint -> compressed content storage to check for similar docs -> can be done offline pruning after the collection of docs 

batch DNS and chaching

for pages in the same host we chache the robots.txt to not fetch it more oftern 

output of url filter to all other nodes to the url eliminator that is in charge of that web partition , other nodes can update it 

some url go through borders of the web partition -> need to communicate through distributed systems 

hash based partiotion -> based on hashes of url
all pages to the same website to the same crawler 

firewall mode -> crawler do not comuunicate links falling in other crawlers space , some urls may never be crawled

crossover mode -> crawler always follow usrl not in theyr space -> can have duplicates 

exchange mode -> communication of links not in theyr space to the correct crawlers 

for continuos crawling :
+ discovery queue
+ refreshing queue -> average page age must be lowe 

for pages that depedn on forms -> creates domain specific queries to add pages 

