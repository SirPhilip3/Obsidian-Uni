*normalized* token > stemming , lemming 

we can define a mapping rule -> from some terms to a normalized one , {usa,USA,U.S.A} -> USA

removing accents 

normalization -> both at index time and query time

remove stop words -> common words

we need stop words for some queries

synonyms > hand crafted eq classes -> color = colour
+ we may want to index also synonims in order to search same phrase even when query has synonims -> blous up index
+ extension query -> query search also for synonims

**stemming lemming**

lemming -> reduce to base form : am, are , -> be 

stemming reduce  to roots -> automate , automatic -> automat

---
**Porter** algo : 
we have rules -> longhest matchin S1 will be applied
S1 -> S2
SSES -> SS    
IES -> I

we can have lenght of prefix (m>2) SSES -> SS , only if we have at least 3 letter before SSES we can use it 

stemming helps with number of results -> harms precision , false positive

---
tokenization -> text is associated to low dimensional dense vectors (embeddings) before traversnig the deepNN

byte pair encoding (BPE) 

initially 1 character = 1 token, 
iteratively merge the *most frequent pair of consecutive tokens* to form a new token -> unitl vocabulary size predefined

---
for IR differnt
BERT -> transfromer for IR 

first tokenization with basic one > splits from points , spaces (does not remove points etcc)
after breaks down words in samller subwords -> WordPieces
vocabulary update with useful subwords

for each token an associated id -> to get from db

dictionaries and tolerant retrieval 
---

tolerant to mistakes on queries 

naive sol
how to store the dictionary -> use array of struct -> with char20 , df , pointer to postings

we can use this in binary search to search in the dictionary

better sol : 
Hashtables -> we cant do tolerant retrieval -> also if the voc grows a lot we may need to reash the table
we can't search for wildcard prefix 

binary trees -> directly on vocabulary -> in the nodes binary test -> leaves full words
we may need to rebalance it 

B-trees better -> we dont need to rebalance-> also better for spacial locality
B-tree lower compression -> with lexicographic order we can do compression 

wild card queries mon* -> we can find a range with binary search    mon < .... < moo
wild card in front \*mon -> harder -> could maintinain a reverse B-tree -> store terms backward and search in this

for the one \*  in the middle we can do and between the two results, set intersection 
EXPENSIVE

trasfrom the queries so that it has \* always at the end 

**permuterm** index 
---

index for the vocabulary still using b-trees 
we put at the end of a term \$ , than we rotate the words to put \$ everywhere -> dictionary increases a lot for each term we store all the rotation

all the rotation link to the original term

h\*e -> at query time h\*e$ -> rotate the term until \* is at the end -> query on this



