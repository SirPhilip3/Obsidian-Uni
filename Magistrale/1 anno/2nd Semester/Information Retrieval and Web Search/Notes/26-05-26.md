BERT bidirectional encoder rapresentation for tranfoermer -> uses encoder only 

uses pre-trained language model 

gpt decoder only transformer -> givea prompt responde with text that maximizes prob 

BERT is a masked language model -> randomly mask one tocken in the inpiut -> ask the model to predict that token given the rest of the phrase 

also next sentence prediction -> have dataset of linked sentence -> like a book . 
given a sentence try to predict next sentence -> not  that we also put random sentence from the corpus as next sentence -> negative example 

fine tuning -> from semi supervised learning step give to the model supervied data this changes the weights 

*tockenizer* -> embedding of the tocken is leanerd while learnign the semi superived model

phrase : add special sep tocken -> to divide two sentences + padding to make sentence match 
\+ add classsification token at the start to indicate start of classfication task -> it returns the class label of the phrase 

bert for reranking 

in document indexing use : 
+ bagofwords 
+ neural retrieval 

2nd stage -> leanr query -doc rapresentation with bert 

given query docs pair we concatename them as : 
+ CLS + query + SEP + doc + SEP 

at the end return 1 prob 

fine tuning -> with relevant and non relecant classifier -> minimize binary cross entropy
d+ -> relevant docs
d- -> not relevant docs 

use passage since in bert number of tocken is limited

bi encoder = query + doc

can use siamese networks 1 for query 1 for docs 

contrastive loss  positive/ positive + negative

dense retrieval -> pass 1 -> use knn search

for each cluster an inverted index -> ids of point in that clustester -> nprobe -> number of closest centroid where to serach ? 

combination of inverted index bag of words 

Learned sparse retrieval 
weights lened by using bert 