neural ir 

learning to rank docs stages : 
1. inverted index -> uses base ranker like bm 25 to get N docs generally a lot more than the actual resutls + query expansion 
2. features + algo used to re-rank the N docs -> get new ranked list -> extract k docs 

learning to rank 

trainig set of queries and ideal doc ranking 

