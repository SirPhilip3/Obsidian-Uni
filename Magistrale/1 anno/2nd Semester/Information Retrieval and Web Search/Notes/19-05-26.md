neural ir 

learning to rank docs stages : 
1. inverted index -> uses base ranker like bm 25 to get N docs generally a lot more than the actual resutls + query expansion 
2. features + algo used to re-rank the N docs -> get new ranked list -> extract k docs 

learning to rank 

trainig set of queries and ideal doc ranking 

features are collected in 3 parts : 
+ query only features
+ query dependent features 
+ query independent -> from the docs

all of them are aggergate to compute a relevance score (q,d)

we can also add new information loke number of images in the page , url click count etcc 

training data -> (query, docs, relevance)
feature vector per query doc pairs -> (cosine similarity , min query window ) , min query windoe is the shortest text span that includes all query words regardless of order 

we can just use linear classifier for relevance predicting decision sourface for $a \alpha + b \omega = \Delta$ 

pairwise preference -> i prefer a doc in respect to another one for a given query 

listwise -> we are given perfect ranking given a query -> object maximixe the whole resulting ranked list 

---
bm25F -> multi field docs not only the text 

F_t -> summ of the fomula for each field of the doc

find best 2s +1 params -> bad when s is big 

NDCG -> top result more important than low

deep learning
---

distributional semantics -> closer words give meaning to that one word

word2vec -> given a center words and conetxt words calculate prov of the context given the center word -> using the density of these words arounf the center 

