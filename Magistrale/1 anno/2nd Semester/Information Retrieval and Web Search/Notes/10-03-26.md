nextGEQ(p1, p2\[j]) -> returns pointer of the first id in p1 that is greater or equal of p2\[j]
cna use binary search / we use *skip pointers*

skips instead of doing it 1 at a time 

skip pointers -> added pointers to some elements on the list that we can follow to skip to some position

skip pointer indicate that all elements between that one and the pointer is between the starting number and the pointer number

expensive to store if we compress blocks we dont need to decompress the full list we decompress blocks only if need to search in it and not skip it 

**where to place skips ?** 

more skips -> more likely to skip but we skip less space , also we need more space to store the array

less skips -> less likely to skip swip more space , less memory concerns 

list of lenght $n$ we place evenly spaced $\sqrt{n}$ skips 

boolen or
---

output size -> worst case where disjointed -> O(x+y)

algo :
if same position are the same -> append one of the two to the solution 

if el1 < el2 -> el1 not present in el2 -> append el1
else contrary 

if one of the 2 are empty append the remaing of the other one to the result

no advantages to use skip pointer

*NOT* 

...

Query optimization -> what is the best order -> example with n times AND

best startegy start from the smaller set -> will delete more sets from the other ones 

general optimization -> process by increasing order of OR -> with document frequencies

---

boolean seach can do also exact search 

boolean is insufficent to capture the lenguage complexities , no control over the size of the results 

Proximity search and partial match

