`*$* -> *`

`X*Y*Z$ -> Y*Z$X*` Z\$X -> isthe same as X\*Z
`Z$X*Y*` -> first look for Z$X* and then filter out those that do not contain Y 

k-gram index 
n-grams -> sequence of n characters -> enumerate all the possible accouring in word -> produce sequence of n-gram

to signal end / start of word use \$ 

new inverted index -> lexicon becomes the bigram 
there can be words repeated in the postel list

processing wildcards now with more asteriks

mon\* is separated in all possible bgrams -> `$m AND mo AND on` , and can be done very efficently
we can return false positive -> ex we can retrieve `moon` -> to remove just filter out with prefix check

only surviving one will be looked up in the inverted index
we can manage middle wildcard 
`mo*n -> $mo*n$ -> $m AND mo AND n$` -> in this case we dont filter out 

permuterm -> (n+1)(n+1) char to be stored
2 grams -> (n+1)\*2
kgrams -> (n)\*k -> note if k = n it becomes permuterm

**spelling correction**

spelling check in docs and user query 

2 spell correction :
+ isolated word
	+ from -> form cant detect
+ context sensitive
	+ i flew form ... -> i flew from can detect

retrieve docs with the most likely correct documents
or return suggested alternative queries

edit distance
weighted edit distance
n-gram overlap -> reduces search space 

select the one nearest 
same distance -> maybe use the most common

*edit distance*

given 2 strings minimum number of operation to convert from one anoter

operations : insert , delete , reaplce (coplex operand :  transposition)

found by dynamic programming algo O(|S1| x |S2|)

algo : 
#todo 

expensive to apply to every possible words 

weighted -> depends on the prob that its misspelled -> like more easy to misspelle keys closer in the keyboard

given misspelled word enumerate the correct until a given distance , very expensive

implement some euristic to cut candidate terms -> generate small edits and intesect with the lexicon

n-gram overlap -> correct and misspelle share lots of ngrams
given a misspelled query (if it doesnt appear in the vocabulary) get all ngrams nd retrieve from the ngram index the lexicon from which to search for min distance 

filter out a threshold of minimum matching ngrams 

*jaccard distance* intersection / union -> 1 when same elements , 0 when disjoint
if j>=0.8 declare a match

