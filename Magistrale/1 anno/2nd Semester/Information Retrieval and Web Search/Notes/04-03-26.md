---
~
---

boolean model -> no ranking 

collection : set of documents -> static -> also the index is static
goal retrieve docs relevant to user needs 

nl input -> simplyfied -> keyword based ad-hoc query

precision mesurements : 
+ precision -> docs relevant -> relevant/tot result
+ recall -> if we know all the relavant docs -> relevant/all relevant -> generally not avilable 

boolean model
operators : 
+ or
+ and 
+ not 

# text documents

trasforming text in numeric vectors tckenization 

-> text divided into words 
-> segment the text extracting some n-grams (sequence of characters) 

ohe one hot encoding

presence matrix -> 
rows : words
cols : text documents

0 word not present 1 present
document vector = col relative to the docs -> what it contains
each row docs that contain that word 1d tensor

queries -> using 1d vector to perfrom bulean operation (not = complement of the vector)
we get the doc

boolean expression to Disjunctive normal form

on big number of documents -> we get a sparse matrix -> better record only 1s position

## inverted index

for each term rapresent only 1s -> list of docs that contain that term *docID* = posting list we can add frequency or positional index 

files -> tockenizer -> normalizer -> inverted index 

normalization :
1. lowercasing etcc
2. stemming > store only the prefix 
3. omit stop words loke "the a to of" -> generally one term  in the vocabulary sintesize it 

stream of pairs of term + docID for each tocken read -> we store them like a stream
than sort pairs by term and than docID 

than for duplicate terms we remove for the same docs if in different docs we save a list 

dog -> 1
dog -> 1
dog -> 2

becomes =
dog -> 1 -> 2  

length of the list is document frequency for that term

## Query

how to process a boolean query

### AND processing

x AND y : 
1. retrieve posting list for x and y (binary search)
2. do the set intersection of the 2 

coplexity $O(x+y)$ -> we need to have the docID sorted 

output size = worst case $min(x,y)$

