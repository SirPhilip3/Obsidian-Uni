context sensitive spell collection 

use sorrounding context to correct the spell -> retrieve dictionary terms close to every term in the query 

in principle we dont know the not correct words 
we suggest the alterantive lots of hits in the documents , less expensive than llm

maybe insted of most frequent in the docs in the *query* 

or break the query in biwords 

llm approaches can avoid use disctionary, since uses prob distribution we give them the qeìuery and return the most frequent

BERT model -> masked labguage modeling -> filling the blank method -> we give the query hiding suspicios word -> fill with the correct one -> looks on all the text 

Index contruction
---

random access better -> ram
spacial locality very important -> we access blocks of data

RCV1 doc collection

stop words ony counted once

the sorting is a lot -> 100M items to sort 

parsing (parser / tokenizer) docs -> given a docs stream we produce a stream of (term,docID), we dont produce the vocabulary directly we can do it after

or we can produce a lexicon and than produce (termID , docID) we dont repeat and also no string , also much faster sorting integers 

we can also add a docFreq to the termID

size of parsed output -> 3rd option with non positional = 12 bytes each 3 integers of 4 bytes -> 1.2GB

since with occurance we dont keep duplicates lower size 

*in memory sorting* 

we define a block small enugh to be stored in ram/cache 
accumulate postings until block size is reached than sort them in ram and write intermediate results on disk

at the end of the stream merge the blocks

binary merge -> every time we do one we duble size of block to be written on disk

multi-way merge -> instead of reading pairs of blocks -> out of memory merge -> the blocks are sorted -> to perfom the merge we dont need to read all -> we just need to buffer small chuncks -> only top position

chunks small enugh to be stored in main memory ram

https://satyadeepmaheshwari.medium.com/sorting-large-datasets-with-limited-memory-the-chunked-merge-sort-approach-318275275c81

storing the vocabulary -> can be large
dictionary for each blocks -> do not sort -> docID is naturally sorted since we read sequentially the docs

