reorder docid so that we can find areas with more 1's 

must apply same order to all the postings list 

using similar algo to solve TSP in order to maximize the docid clusters by reassignemnts of columns 

still need to calculate distance-> jaccard distance = 1- jaccard sim 

tsp on the docgrap

tsp doesnt scale well -> clustering 
1. cluster document collection
2. reorder the clusters not the documents
3. tsp over the centroid -> assign docid to docs based on the cluster ordering given by tsp , inside the cluster random assignment or still jaccard distance 

clusteing is done while reading the docs so first will always be a center , to kmeans we add radius -> max radius of the cluster -> even if should be in a cluster it will become another one if sufficently distant from the center tuning radius = tunes number of clusters 

first cluster bigger than find smaller cluster inside ieach -> than tsp for lower level and than for higher 