instead of encoding in gamma G *+1* add a bit to remove not prefix free ecnoding 
2 for gamma 10,0 -> delta 10,0,0 -> lenght of the ofsets when is 2 or 3 -> should be lenght 1 and 1 in gamma is simply 0 we istead add a 1 to make it prefix free 

in delta we always add +1 to the lenght of the offset to encode in gamma -> in decoding we do -1

but still we keep 0 as encoding of 1 since very common

delta worse for smaller numbers 
better for larger values

**golomb rice** 

not parameter free -> we need to chose a number $M=2^j$ modulos power of 2 , used as divisor of the numbe that we want to rapresent 

split n in two integer components : 
+ $q(n) = \lfloor \frac{n-1}{M} \rfloor$ 
+ $r(n) = (n-1) \mod{M}$

M stored in metadata 

we encode $q(n)$ in *unary*

reminder between 0 and M-1 -> we can rapresent it $\log_2 M = j$ bits

q can be stored in bots unary and alternaive prefix free encoding
q can also be = 0 -> elias delta and elias gamma can not ecode this , to remve this we encode q+1

reminder in binary normally since max is M-1

**PforDelta**

compresses insteger sequences instead of single integer , we dont need prefix free encoding since we always use the same bits for a block and integer

take a big sequence of gaps -> *most* gaps must be between an interval I \[base , base + 2^k -1] 
remove the base from each block -> becomse \[0, 2^k -1]

base stored as metadata

k is tuning param -> the numbers that fall outside will be exceptions -> go to the overflow list

special symbol to rapresent exceptions -> reduce from the expressable numbers -> the maximum value will be used as the special symbol 

pfordelta is faster because it can leverage SIMD operations on the cpu but worse for compression 

**Interpolative coding**

encode strictly monotone sequences -> exploits 
we store the starting and ending id and lenght -> we can materialize the original one 

where len = to them max -> best situation since gaps is always 1 

recursive encoding -> split the sequence in 2 halfs and apply same method to each of the 2 

subsequence with gap = 1 -> stop the encoding len = max -min 

len first and last use delta or gamma 

-?