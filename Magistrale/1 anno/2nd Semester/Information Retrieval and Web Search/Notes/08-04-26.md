interpolative coding 

recursively spits in half -> binary interpolative coding

instead of using gaps -> encodes position in a integer range

elias gamma optimal when most probable is present half the time seocnd most will be 1/4 etcc -> maybe ??? 

*vector space model* 

no more boolean retrieval 

now ranked retrieval -> boolean will return unsorted results , useless with huge db 

free text queries -> simply phrases

at each result associated a score -> we use it to return only top k result

after top k results we do another reordering 

to each doc some score -> typically similarity measure -> cosine similarity

why not using jaccard similarity ? 

more frequent the query term present in the document the higher should the score be 
not including stop words 

rare terms are more important -> they are more informative than frequent terms 

keep also the term frequency not only the presence 

$tf_{f,i}$  

bag of words -> set + cardinality

tf idf

more score to docs containig 10xapple -> log transformation 
not considering the lenght of the documents

document-query pair score -> sum over therms t in *both* q and d of the tf_t,d

inverse document frequency -> more important rare words 
stop words occurs in all docs -> 0 value

idf depends only on the corpus of the documents 

scoring the same independently from the document -> wont change for different docs 

tf x idf -> final formula 

final score given query and document -> sum of the terms occuring in both doc and query of the tf x idf 

