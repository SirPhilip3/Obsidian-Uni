ndcg example 

using user clicks 

kendall tau correlation 

1 perfect agreement , -1 disagreement -> opposite 
0 -> X and Y independent 
X = number of agreemetn 
Y = number of disagreement

t = X-Y/X+Y

---
relevance feedback and query expansion 

improve recall -> more relevant docs ? 

if we serach for exact match we may miss some docs that instead have synonims of that word > we need query expansion 

rocchio feedbakc -> interactive relecance feedback -> telling the ir sustem whick docs are more relevant ->dinamically -> rocchio feedback 

user needs to mark some as relevant and some non -> new query with better result 

query expansion -> improve before submitting query -> add synonims etcc

---

rocchio tries to find the optimal quert for relevant and non relevant docs
maximize similarity with relevant documents while minimizing similarity for non relevant ones 

pseudo relevance feedback remove the part where the guy needs to do this is good or this is bad 

uses hits from the user given a query -> assume that the top k hits are also the top k relevant docs for a given query 

---

query suggestions by thesaurus based -> use synonims , can harm precision since returns more docs in general 

automatic thesaurus genaration 

similarity between 2 words :
1. way > similar if they co occure with similar terms car similar to motorcicle since the appear togheter with road gas licence 
2. similar if the occur in a given grammatical realtion with the same words
	1. ex both apple and pear are linked to harvesting , peeling , eating etcc 

dsitributional semantics -> meaning given by words close by 

word2vec