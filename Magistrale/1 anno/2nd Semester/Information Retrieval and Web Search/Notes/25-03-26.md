distributed indexing 

open and closed questions midterm 22/23 april

docID assigne by the parser 

token in the pseudocode is term + docID
posting list variable array 

compressing terms and postings 

distributed indexing 
maintainig a master index is failure point, break up in parallel tasks , master assign a task to an idle machine from a pool

2 parallel task -> 
+ parser -> trasfrorm in pari docsID
+ inverter -> creates inverted index from intermediate results 

we store terms separated in partition regarding the first letter

inverters take the blocks of a-f etcc from all the parser and produce postings relative to them reduction

more splits than node per parser 

dynamic indexing 
on the fly add , deleted or modify a docs

simplest approach rewrite the index after some time

or maintain big full index and auxiliary smaller index for changes 
for deletion invalidation dat astructuer corresponding to that one

merging expensive 

the index is in one big file -> fuking ass 

sharding :
better scalabilty , performane and fault tolerance 

document based partitioning -> each index produced by distinct split of the collection also vocabulari different 

pages divided between nodes -> at query processing we need to locate the correct node 

term based partitioning , fully disjointed between  -> this can be done only after building the global terms 

query processign -> we can need to access lots of nodes for a query

more traffic = more replicas 
larger index = more shards

index compression

how to compress dictionary 

case folding removes word from vovabulary but opsting list doent really reduce since we need ot do like incude id not present in the first or second one 

positional index -> do not observe any reduction since the positions are teh same 

removing stopwords not really changes the size of dictionaryy but in the postings its difference , we remove long posting list