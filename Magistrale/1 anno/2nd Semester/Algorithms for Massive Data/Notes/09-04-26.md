concentration bounds 

randomized algorithms to estimate stats of some data string
generally E\[X] equal to value that we want to estimate

ideally output of the algorithm should be with low variance 

concentration bounds will helps use bound var between some threshold 

epsilon approximation

$$
|\epsilon \cdot E[X]|
$$
markov bound -> linear dependency 
chebychev -> quadratic 
chernoff -> exponential -> stringhest upperbound -> but has some assumption on random var 

weak assumpion = weak bound 

**Markov**
+ assumtion $X \geq 0$ 

$$P(X \ge a) \leq E[X]/a$$
works only on non negative since in the proof if we take out some a and this is <0 you will have larger value not smaller 

linear upper bound 

*example*

the prob of getting face 6 at most 0.58-> but actual is exactly 1/6

50/80 -> 0.625 -> at least 80 head -> quite unlikely 

**chebychev**

*assumption* -> we know the variance of the X , works also with negative rand values

quadratic bound 

sum of variance of ind -> at most sum of the variances 

var bernu -> p(1-p) = 0.25

P(|X-50|>=80)

0.5 since ber same for <50 and >50 , centered in 50 

*boosted cheb* -> run multiple times and take the average -> repetition of s time

**chernoff**

assumption -> rd -> sum of independent and identically distributed bernullians 

$$
P(Y \ge E[Y]+t) \le e^{\frac{-t^2}{2n}}
$$
right sided

not much much better than cheb sicne small number of repetition 

**mmtrick**

assumption variance must be O(E^2) at most

start from error rate and failure prob tells us how many time we have to sample 
t = log 1/ delta , s = e ^ -2
we do st draw of the rand var 

for every col compute the mean 
and compute the median of the means 

boosted chebichev -> sicne the mean of rand var 

replace 

if median fail also the other larger fail as as per too small -> when median fail at least half fail 

sum of bernullian , success prob is 1/3 -> apply chernoff 

ex
---

2 strings lenght n alpha Epsilon

compress them to calulate hammin distance -> numb of difference divided by n

random number between 1-5 -> scheck will be the number we in that point on each string -> const size 

relative hamming distance will be hamming distnace between the 2 chars 
d(x,y) = ber 2/5 -> prob that i get 1 by sampling 1 char from the two chars 
-> Ber(d(X,Y)) , X and Y orig strings 

E\[d(x,y)] = d(X,Y)

now extract k chars -> prev still holds 
d(brdb, bbdb) = 1/4
d(X,Y) = 2/11 

take average between k rep of random var the expectation doenst change 
