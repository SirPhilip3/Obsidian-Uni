compressing integers 
$$
\log \binom{n}{m} + O(m)
$$
instead of n

sligtly slower queries 
access = O(1)
other log time
rank access select0 -> log m , select 1 instea O(1)

if sparse very good

count integer from 0 -> in binary ez, n will be a power of 2
m also power of 2 

good in both sparse and dense
# Elias-Fano

sort set in increasing order
write number in binary -> log n bits

break in prefiz of log m bits and the rest as suffix

EF1 = store explicitley the suffixes -> bit packed m \log n/m ->wrong in slides

compressing the prefixes -> number in nondecreasing order form 0 to m excluded

take the diffrences between the numbers (bewtween the prev) except the first one 

EF2 = encode the diffrences in unary -> y rep of 0 followed by 1 -> 2 > 001 , 1 -> 01 , 0-> 1

if you count the number of 0 > the original prefix

EF2 -> where we need rank select queries -> RRR 
number of 1's m
0's -> largest prefix we can have -> 2^{\log m -1} -> <= m -1

EF2 -> 2m -1 -> 2m+o(m) -> safe upperbound

---
main query -> get ith integer, ith smallest integer, suffix just get the ith
prefix -> jump to ith pos in ef2 and count number of 0 before

rank0(select1(i))

predecessor -> binary search

to store inverted lists
