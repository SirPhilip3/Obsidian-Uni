---
~
---
Wavelet trees 

SA+text compressed to $nH_k + o(n\log \sigma)$ for small $k$
count $O(m \log \sigma)$ 
locate $O((m+occ)\cdot (\log n)^{1+\epsilon})$ 
extract ....

Wavelet trees rank, select access queries solved in O(log sigma)

Barrow wheeler transform 

Wavelet trees
---

binary tree , sigma leaves , balanced tree -> log sigma height

inside each node a bitvector

queries solved traversing WT doing RSA queries in the nodes 

example FACE BAKE CAFE sigma = 7 , n = 14

log 7 = 3 bit encoding 

![[Pasted image 20260326155524.png]]

each line = level of the tree

0s to the left 1s to the right -> partitionig the character in 2 sets from left to right
do the same for the 3 levels 
for the last divide the two letter remainig depednig on 1's and 0's of the last level, last level chars stored in the final data structure

the tree looking bits on the edges = encoding 

$ = 00
A = 01
B = 10
N = 11
```
BANANA$
1010100
0111110
```


![[Drawing 2026-03-26 16.02.18.excalidraw.png]]
%%[[Drawing 2026-03-26 16.02.18.excalidraw.md|🖋 Edit in Excalidraw]]%%

we just store the bitvectros instead for the layers 

number of bits -> n log sigma -> sigma leaves , nodes -> 2 sigma -> O(sigma) 
log n = pointer size 

total size nodes O(sigma logn )
space leaves sigma log n 

main number of bits in the nodes  

exctly the space of the string
using RRR nH_0(S) -> zero order compression locally propagates to the whole string 

**proof by induction** on the height of the tree 

base h=1 -> just the bitvector -> already reaches H_0 

assume true for height h pruve the claim for h+1 

S storing the whole string rapresented by tree of h+1

N_0 and N_1 number of 0s and 1s in the root, 

alphabet divided in char that go to left sigma0 -> statr with 0 and right sigma1 -> start with 1 
number of occurance of c that start with 0 same in S and S0

induction hyp on left and right gives us theyr zero order entropy 

