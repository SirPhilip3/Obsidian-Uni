compressed suffix array

CSA 

we can compress SA + text in nH_0 +O(n) and solve 
+ count O m log n
+ locate O (m + occ) \* log n -> slightly slower
+ extract O(log n + l)

we dont need the orignal text

we can do also without this :
sigma = alphabet size <= n / log n -> simplifying

>[!note] 
>not true for permutations of the numbers from 1 - n

![[Pasted image 20260323155533.png]]

if we have another way to extract suffixes instead of looking in the text 

build it : 
+ store first cahr of every suffix in sorted order -> lenght of string log sigma for each bits but F is *sorted* so 

good sol -> mark with RRR bitvector the first occurance of every character + store 1 occ of every character in a vector in increasing order

rank_1(points) -> than access the alphabet array 

getting the other charcters -> the character in other level are actually in the previous ones 

store the psi function > from a suffix to the ones that we obtain by deleting the first character

psi\[2] = 1 -> the array resultin is compressible
SA^-1 position in the suffix array where the number appears

exercise 
1. 1 , 7
phsi -> next positon of the text 

12 11 8 5 2 1 10 9 7 4 6 3
look for the next position 
ex : position of 
nd,1,8,11,12,5,2,7,3,4,9,10 #todo understand

 S = ABABB$ ($<A<B)
SA : 
$ -> 6
ABABB$ -> 1 
ABB$ -> 3
B$ -> 5
BABB$ -> 2
BB$ -> 4
for psi just look to where n+1 is in the SA -> ret position 
nd,5,6,1,3,4

**last** 
**psi-1 ->** 
**nd 1 5 2 4**
**nd must be 5 -> than look at position-1**

how to extract suffixes given only psi F 
ANA$

pos 3 -> psi\[6] -> 2 

---
compression

cluster of the same character -> increasing order for psi 
each next prefix will be larger than the previous one so in the next block linked we will jump always to increasin number since also that is sorted 

order preserved if we remove first letter

compressing this sorted list of integer with Elias Fano -> allows access in constant time 

m log n/m + O(m) -> ex m = number of A's -> total univer still n> length of string sum for everythnh -> mult and divide by n outside and inside summation O(nc) -> length of string O(n)

$$
n \sum n_c/n \log n/n_c +O(n_c)
$$

put them togheter

FO = 1100110
V = \[EF$,EFA,EFB,EFN]

want 4 -> rank -> get 2 -> jump 2 in V -> in position rank_0 on the position and on the rank_1 -1 -> removes the previous 0's 

locate and extract we want to just add some small space 

locate we need another way to compute SA\[i] -> if we store n/logn el of SA

sample 1 out log n SA values -> now we need to apply psi at most log n before hitting a sampled value 

appllying psi we move forward in the text 

keep SA\[i] only if divisible by log n or the last one 

bit vector for sampled values
SA\[i] = SSA\[M.rank_1(i)] if sampled otherwise not sampled 

same tecnique for extract but we sample the inverse suffix array 

M just takes o(n) bits 

not nice for dna nice for large alphabets 