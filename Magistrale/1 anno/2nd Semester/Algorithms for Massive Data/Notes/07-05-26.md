streaming comp model

feaures :
+ max working space
+ delay element time per element processing 
+ prob of obtaining correct sol 
+ approx ratio

pattern matching recieve : string n+m 
+ patter y_1 y_n
+ text x_1 x_m

easy to do in n working space just store window of pattern 
solve it in log n space 

---
n working space 

poly hashing -> 1 mem word 

issue is updating information about the hash , give the previous hash 
we need to acces the n character in the past -> should store the last n char O(n) space 

porat porat
---

while pattern arrice compute hashes of pow2 position -> in log n space -> simply appending 

text arrives 

keep log n sets W0 - W3
Wi stores occ og the prefix 2^i in the last 2^i+1 char

suppose new char arrives 
1. windows to the right by 1 position and update Wi
	1. check if exits from the window
	2. insert new matched pattern
	3. if in first cell check if they can be promoted in the lower window , check if occurance of the next level 

promoting the last means i found occurance of the pattern 

wi constant space : 
A = 2^i inside B=2^i+1
3 occur of A -> origianl string B must be periodic

tey need to be regularly spaced

rappresentation of pattern :
(r_1, t,p) 
t = cardinality
p= pattern
r_1=first occ

removing r_1 -> (r1+p,t+1,p) O(1)
appending (r_1,t+1,p) O(1)

hash of the last 2^i+1 char only on promote

keep 
+ hash whole string -> updating it just do in the polynomial as bef
+ hash of the text between first included and sec excluded in each W_i > 2 -> only if there are more than 1 if there are 3 and one goes out the hash for the new part is the same since the patter is periodic , 1 before new one arrives from the top -> need to calculate the hash

We dont have the hash we only have the hash from clostes occ to the right to the end of W
compute with next hash - total - start
+ hash from beginning of string to 1st occ -> this changes when one elment exits from w , but we have the hash from 1 to 2 and concat the two , if one appears full string hash - the one from the prev level -> ( all - prev level ) * z^-len of prev lvel

