frequency estimation 

fully determ freq estimation -> we can only underestimate 

keep a sample of elements MG as hash table with associated frequency -> largest sample s-1 

if inside the sample > z frequency otherwise -> 0

if we try to inser but maxsize -> decrease all elements by 1 
for all element that have freq = 0 we delete

will remove at least on element since the pass before we added on element with fq=1 -> fq=0 removed it 

estimated freq alwys larger or eq to freq - m/s (s = size , m=lenght of data stream ) )

each time we arrive at |MS|=s we decres s frequencies -> all -> why cant this happend m/s times ? 

if instead of usin freq we put copies of the values -> each time we delete s elemetns -> since at most m elemenents > cant delete m/s batches 

**complexity** of freq estimation -> constant time 
of insert -> O(s) -> e^-1  -> global counter decreased by 1 -> freq i see on the bable - the counter

nested doubly linked list 

![[Drawing 2026-05-25 16.15.40.excalidraw.png]]
%%[[Drawing 2026-05-25 16.15.40.excalidraw.md|🖋 Edit in Excalidraw]]%%

ret 0 = freq smaller than m/s

disadvantages -> not linear no addind schetckes 
not ez to integrate in hardware 
deletes not supported

---
(5,2)(1,1)(9,1)

---
improving CMS -> uses integers instead of pointer in MG, counters can be approximated not pointers

approximate counting 

store exponents instead of number -> 2^x ->just store x -> if we know that power of 2, otehrwise store x = log 2 x ceil 

y is less than 2^x and more than 1.5 2^x 

base 1+epsilon(epsilon = error) -> 1+ espilon approx 

cant increment 

algorithm :
+ initializato x =0
+ with prob 2^{-x} do x = x+1
+ estimate 2^{x} -1

2^{X_i}

---
dont increment = same parameters , otherwise increment

fix 1 number to gi