approximate pattern matching on streams 

*hamming distance* :
+ 2 strings > number of differences 

we compute hamming distance exactly with some prob but within max k 
result :
+ space and delay poly(k log n)

2 strings partition in t sub sequences larger than k, each pair contains at most 1 missmatch -> each in one class of the partition -> number of missmatching = hammin distance 

3 cases 
+ distance = 0 A=B
+ dist = 1
+ dist > 1

A i d -> seq A\[i] append A\[i+d] append A\[i+2d]

the miss match will appear in of the partitions, hashing checks

2 miss match in the same partition -> H=1-> ony on pair missmatches but realH = 2 

if distance between missmatches = mutiple of d in the same partition 

use more partitions

since we dont know A and B ->we dont know the distances

first log_n prime numbers , for every pair i ,j at least one prime does not divide |j-i|

try all possible prime number and return prime such that highest missmatches 


streaming algorithm 
---

text char by char -> duplicate char log n times , divide in d substrings depending on each copy relative d 

than do porat porat fro each combination 

new char arrives -> alterante the matching 

at teh beginning we know lenght of pattern so the d's remain the same 

delay , when new char arrives only log n prp instances changes -> log n delay for porat porat, total logn^2 

space poly log 

gen for larger k 
---

generalize the sketch 

k miss matches at most k^2 missmatches (all possible positions)

try all the prime from k to k^2 log n intersection n

polynomial in k log n

exercise
---

`I_AM_NOT_A_ROBOT`
`1_AM_N0T_A_ROB0T`

3 ,  k^2 log n
9 log 16 
1. 3->13 primes

2. 6, 7,11 -> 13 \ 5\ 11 -> 5 

3. divide in 5 pairs 


edit distace
---

guess the syncronization between 2 strings -> 

flip a coint and decide if we want to increase counter 
haming distance always >= k, wiht prob 4^-k exactly k 

when hash same input = sync , if hash return true stay , otherwise move to the next char 
when we have differnt input we could move in different ways -> fully independent from one another , if we want to sync 1/4 prob 

for every edit we want to keep sync -> 1/4 prob 

we just repeat this -> so that at some point we will be lucky and catch the right hamming distance 

works only with small k