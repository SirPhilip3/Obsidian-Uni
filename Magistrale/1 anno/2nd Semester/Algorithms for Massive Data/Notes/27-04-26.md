counting bloom filters 

normally no delete 
instead of storing bin store integers

same as bloom filters , add element = +1 , delete an element -1 
deal with underflows and overflows if fixed number of bits 

prob of a false neg -> >0

example build
2 params = k , M(mult of k) M lenght of array k numb of hash functions
t -> number of bits per integer 0-2^t excluded

total space M\*t

like before h indexes in CBF 

in the example -> 2 bits per integer

$min (BF[h_i(x)] + 1,2^t − 1)$ -> to not overflow if > than the max number storable will remain there 

retrieving the number of elements can't be done

membership if >0 true 

delete -> do -1 but dont go below 0 no underflows

fn since removing overflow removes some counts 

---
prob of false positive and negative and give desired prob 


|     |     |     |     |     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1   | 1   | 1   |     |     | 1   |     |     | 1   |     |

2 -> yes
3 -> yes 
6 -> yes FP
del 5

|     |     |     |     |     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1   | 1   | 0   |     |     | 1   |     |     | 0   |     |
3 -> no
del 3

|     |     |     |     |     |     |     |     |     |     |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1   | 0   |     |     |     | 1   |     |     | 0   |     |

---
a given positions overflows -> while inserting the integers than it gets incermented at least 2^t times 
since i is fixed only 1 function can affect that entry 

while inserting integer in the bloom filter apply hj to them -> name to each position 

iid distributed -> uniform distribution 

P(i_p = i) = k/M

among all pos at least T of them are equal to i -> that i will increment CDF(i) at least T times

all rand variable = i -> prob k/M^T , : existance 

bin coeff

smaller than false positive -> even with small integerers 

size = M t = m k log2 e \*t
FP 1/2^(M/m) ln 2

---
exercises 

fun -> log_2 (1 / 0.01) = log (100) = 6.6 -> 7 
integers -> 7 -> functions -> m k log_2 el = 

union between 2 counting bloom filter -> + between the two counting -> counting overflows by capping them 