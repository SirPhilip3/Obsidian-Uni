schetcking 

frequency moments 

estimate individual frequencies 

counting distinct elements 

+ n -> bitvector 
+ d log m bits -> exactly -> presence store number
+ minhash bad bc d +- e * n  where n can be hughe is bad

nor d +- epsilon * d 

no uniform sampling -> all the same -> rare elemetns count as much as frequent elements 

flajolet martin
---

approximated streaming algo 
hyperloglog

unifromly at random d elements , sample more lements than smallest one may be even smaller then the one before

keep the minimum 

doesnt care if elements are repeated since the algo depends only on the minimum non effected

element not uniform -> randomize with hashing 

the more distict we see y smaller -> so the recip will be higher since we see more distince number 

on expectation the will spread regularly on the unit line, expectation of the smalles 1/(d+1) solving for d get that -> but in the formula we get directly y expectation doesnt commute for division 

*biased* estimator if y1 low variance also everything will be 

1/0.125 - 1 = 7 -> smallest hash  -> run multiple 

analysis
---

integral over continuos domain 

d = numb of distinct elements can disregrad copies -> dependent -> same rand variable 

unbiased + var bound by square Exp -> mean + median 

we run e^-2 log (1/sigma) independent isntances of FM -> y' -> mean +median trick

run again mm trick with goal e/4 instead

*uniform hash function* too much sapce
approximate real numbers with fractions

Bottom-k 
---

intead of y1 take yk and divide by k -> variance is samller -> boosted chechychev 

k/max

0.125 , 0.903 , 0.745 -> 3rd smallest 5 -> 0.745 
estimation  3/0.745 = 4.03

running the *median trick* 


loglog
---

pairwise independent from n to 64 bit inteegrs 

hashes and L = remeber longhest prefix of 0's
retrun c \* 2^L 

why leading 0's 

5 -> 2 -> c\*2^2 ->0.783 \* 4 = 3.132

why works 

random intgere half start with 1 0's , 1/4 with 2 0's
more integer we see more likely we see long 0's -> overestimates -> we need correction 

smallest hash also the largest number of leading 0's 

space -> needs only to keep track of L-> log log d 

Q1 = C

EX1 
1. porat porat +edit distance -> filter effected urls
2. loglog on ip address of the servers  -> count distinct servers since urls can be in same server but different urls 

EX2 
1. 10^6 small server -> each bottok each to central server as stream 

C estimate num of distinct userse that accesse the database

keep the kth smallest global hashses -> k smallest hashes in all the servers 

can be done with same hash functions 