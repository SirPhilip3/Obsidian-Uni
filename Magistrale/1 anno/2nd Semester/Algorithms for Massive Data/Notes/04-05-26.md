locality sensitive search

if 2 obj are similar than collision with larger prob 

main issue is calculating the distance between all elements-> square number of distances 

hash function such that P(hx = hy) increases as d(x,y) decreases -> put in the same backet of the hasing algo

fix a distance threshold t -> collide if disrance <= t > impossible since step function but we can approximate it -> the more we approximate it the more space the hash takes 

2 data points provided to approxiamte function, one close to prob 1 one to 0

def :
- . -

choosing 2 points depend on the application 

---
1. start from straight line
2. 2 transformation to the function making it similar to a step function

transform minhash into a step function

choose uniformly r independent hash functions

AND + OR -> do and than or prob becomes 1-(1-p^r)^b 

r controls the threshold
b controls the approximation more the step function -> large b = larger step 

b determines also how many hash tables we build 


