F2 and dim reduction 

count sketch 

pick hash that outputs -1 or +1 -> pairwise independent , expected prob = 0

E[h(x) * h(y)] = 1 if x=y -> -1 \*-1 or +1 \* +1
x != y ---> 0

count skectch freq on stream

initialization
	h = 
	z = 0 -> com hash of all the el

expectation z = 0 -> sum of exp of 0 

---
how many repetition of element y in the string -> frequency of y -> left side sum up 1 for every occ of y 

error multiplies l2 norm of the frequency  -> in CMS m can be much larger than l2 norm of f

---
tug of war skectch 

F2 -> 2nd order freq moments -> refelcts how much freq are unbalanced 

E[z^2] = F_2

sum for all the universe elements -> freq of that element times its h(x) 

varaince we need four wise independent
express variance as expectations -> will be lt E[z^4] -> pow 4 need four wise independence 

0 when at least 1 element different from the other 
1 when all equal or pair of equals

use mean+median trick 

---
dim reduction for l2 norm -> create linear operator 
for any 2 vector linear operator that maps this tu vectros of dim O(e^-2)  such that M\*x and M\*y distance is (1 +- e)\* l2 norm -> with probability 2/3 this happends -> by repetition higher prob

computing ditance takes O(e^-2) time

mapping vectors to numbers -> preserving square of l2 norm expectation -> 2 number = square of the difference

if we can estimate distance from the origin we can estimate to another vectors -> for linearity

apply TOW even if real numbers than computing z , E[z^2] -> returns F2 or summation of x_i square , wher x is an element in a real vector

z can be rewritten as product of vectors 

if we get a vector h > of -1 , +1 taken radomly keeps the euclidian distance on expectation 

improve precision add rows to M -> entries -1/sqrt(s) or + 
s=num rows

s = 9 e^-2 than prob 2/3 that the trans is epsilon approx 

boosted chebychev -> average of various iteration

---
increase success prob to 1-delta

mutiply number of forw by log 1/delta -> concetration bounds -> gives linear operator

numb of rows controll both success prob and error rate 

---
JL transform 

uses gaussian instead of -1 , +1 -> slower since we need to pick gaussian

1. computing Mx in O(n) -> using sparse matrix on M -> we can ignore 0's e^-1 instead of e^-2 -> optimal for sparsity

2. use dense M but very regular -> product between 3 matrices  -> A diag with -1 , +1 ; B = X Y -> for every entry of Y insert a whole X 







