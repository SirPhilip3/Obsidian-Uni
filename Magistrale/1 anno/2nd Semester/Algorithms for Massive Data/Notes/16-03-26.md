**access** query -> access ant element of C O T in constrant tiem -> to extract -> id the block t containing B[i]

i = 8 , t = i/b = 3
3rd el from C and O 

than T[C[t],O(t)] -> gets block than bit op to get the bit

C, T fixed lenght encoding -> bitpacking 

O has varlenght enco -> we can add small info to extrac in constant time hints 

divide O in groups of b blocks , store the statrgin position of every macro block in the *encoding*

we store starting position of every other int relative to the start of the block

extracting a code with the 2 arrays ->sum the numbers if we need O6 -> 2nd macroblock in the 3rd position 7 + 3 -1

storing 2 arrays more arrays -> needs to stay in o(n)

bits for every offset -> at most b bits

b\*b  -> for every macroblock, number ill always be <=

log n bits for each number > just n/b^2 numbers 
log n * n/(logn)^2 = n/logn 

n/b * log(b^2) = O((n log log n)/log n)

**rank**

same as access -> table R -> all possible rank query -> precomputed result 

T[2,1] = 101
R[2,1,1] = 1
R[2,1,2] = 1
R[2,1,3] = 2 

size of R > 
rows -> b+1
cols -> \leq 2^b
third -> b (lenght of bitvector)
bits for each number  -> log b
O(b^2 2^b logb) = logn n ^2 sqrt n log log n = o(n)

new array rank number that are relative to the begining 

no select

---
compression is local independent on other blocks
bitvectors concatenated 

between B1 and B2 we lose some compression power

blocks can not be aligned with the oririnal bit vector > ignore the block that overlap
the block has zero entropy upper bounded

overlapped cant bound it -> best we can do is logn bits -> we dont compress at all -> at most t overlaps -> t logn 

log n is safe bound it will be compressed in some way 

---
ex

