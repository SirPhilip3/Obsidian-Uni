sketching 

MinHash 

online data sketch of the data 
prop
1. bitsize(f(x)) << bitsize x
2. f(x) used to estimate some prop on x
	+ numb of distinct el in the stream
	+ freq of el 
	+ function of the freq of the elements in x 

generally cond 1 impossible to compute stats for cond 2

3. dynamic sketch, without knowledge of whole input data 
4. similarity preserving -> if 2 similar than also skectches similar (not mandatory)

bloom filter as sketches but in general sub linear space 

ex 1 -> simply store the sum of all number -> if operation is average
ex 2 -> min/max keep only min max of elements 
ex 3 -> number neg or positive without 0 , seq of operation on multiset , + means add that number - remove -> *poly hashing* -> empty poly is 0 ,
	inserting an element x = k(S) + 2^{x-1}  mod q
	removing an element x = k(S) - 2^{x-1} mod q

similarity preserving -> preservs jaccard sim, hammin , euclidian etcc 

jaccard similarity with *minHash*
A uniform 
minhash -> depends on hash funcition -> $\min {h(x) : x \in A}$

set $A = 4,2,6,1$
$h(A) =2,3,6,5$
min = 2 

given two sets A B are = with prob J(A,B)

create indicator bernullian varible 1 if the minhashes match 0 otherwise , expectation of bern variable is Jaccard -> proove

repeating extraction of bern -> absolute e approx

proof

size of union = N
look at all hashes in union (apply) if i elemetn with smalles has smallest(i)=true, since h perm jsut 1 minum unique 
p(smallest(i)) = 1/N

law of total expectation -> partition the expectation of rand var

---
estimator is bernullian 0 or 1 not good for jaccard since its number between 0 and 1 

apply chernoff-hoeffding -> repeat k times iid and compute average 

storing permutation --> lots of space 

---
storing the function log n! -> rand perm too much space 

min wise indep permutations 
some function from 1-n to 0-M 

must be true for any possible set 

cardinality of H must be a multiple of A otherwise no minwise independence ,
must be true for all possible subsets A all the number from 1-n -> e^n 
so card at least n bits appl log , still not good 

better approximate min wise only log 1/epsilon

---
exerc1 use min hash -> checking just poly hashing  

exec2
how many pc packet to both A and B 
sets 1 per pc with list of packets -> intersection -> result 
if we multiply jaccard by union -> min hash for jaccard and than multiply it by the union -> n 
good for big intersections 