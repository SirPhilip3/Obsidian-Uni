fm index

count -> *backward search* n log sigma time 

given BWT 

assume we have the range of ab -> we want aab range 
looking in BWT -> the prev is a -> and do LF mapping at the start in the original

find a string

every step needs to be log sigma 
lf mapping only first and last

rank_a(start-1)+1 prev position otherwise wouldnt count it ..z
rank_a(end) 
empty = beginning after the end

to get the range in the FR > array containing starting position -1 of each symbol 

*fm index*

sample a bit more values than normal csa 

chose costant > epsilon > 0 as small as possible and alpha < 1

count O m log sigma -> backwards search
locate O m log sigma + occ ....   -> backwards search + smth

log n ^ 1+epsilon > walk past sample to find string

---
with sa faster

3rd ex
runs = compress more than 1 same close in 1 char 
elias fano + letter + wt 

----
not in course 

breaking entropy bound
compress with run lenght encoding

bwt cluster equal letters toghether, since distance between groups is very similar ?

backwards search causes cache misses seach each time we jump in "random" places in bwt

fm index for arbitrary graphs -> sort the nodes in co-lexico of the strings entering the nodes -> total order is SA 