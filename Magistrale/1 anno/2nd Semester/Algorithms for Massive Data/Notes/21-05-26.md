frequency estimation on strings

given a stream of integers -> estimate for any el of the universe the num of occ in the string

node works bc sampling maintains frequency 

1. sample unif at random q elements 
2. possible to extract on the fly 

estimate : frequen in the sample scaled by q 

(x=y) -> indicator 0 if diff 1 if eq

estimator needs to be unbiased 

prob that estimator deviates for some epsilon \times m -> for small (what?) is bad

rand of bernullian -> chernoff hoeffing

minimize fp 

sampling choose erro and fg -> sample  2e^-1ln2/delta 

additive error s-> added to the fy -> f1 + em
multiplicictavie error fy +- fy e

the sketch size prop ro e^-2 e must be at least 1/sqrt(m) otherwise e^-2 > m

root m bad  

count min -> alwyas true estimator >= real value -> only over estimation

CM matrix t rows s col 
every row with universal hash function -> prob of collision 1/s 

init to 0

insert like counting bloom filter wihtouth checking overflow

return the minimum from each row with col selected by hash 

--- 
off by m/s -> many cols we chose the less uncertainty -> bound on expectation -> on the rand variable -> markov concentration bound 

all the rows exceed the error with ptob 2^-t 

number of cols 2/e
rows log(1/delta)

---
count min with conservative update -> we just increment the smallest cell instead of all 

10^12 * 8 -> 

2.65 * 10^11

---
counting scketch with range

not adding freq -> huge errror 
log n dyadic intervals

intervals of pow of 2 that cover the whole universe 
level 1 -> counting sckecth on all 
level 2 -> on pairs etcc 

dyadic intervals > every interval covered by 2 log n dyadic intervals 
