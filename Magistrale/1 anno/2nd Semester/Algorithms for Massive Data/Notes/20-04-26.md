hashing

input is not random typically -> we make the input random -> by applying hashing
\+ reduce theyr size
choice of function is random from a family of function
only at the beginning -> only randomized step

h : U -> \[0,M) M excluded 

since output depends on random variable , also output will be random variable

depending on the family H different properties

h(x) must be fast to compute
space guarantee -> should be small

storing function worst case entropy 
so it should be smallest possible cardinality 

M^3 > each integers takes can be as large as M 

the bigger H is the more random the h(x) -> if |H| = 1 -> H not a rand variable 

max randomness H = 0, M^U ;;; U=0, M > all possible functions

uniform distr h(x)
indipendent also between each other 

*completly uniform hash family* > fully uniform 
getting any possible tuple is uniform

---
if you pick any other family than we can't get full unifromity ( only fully uniform is H = 0, M^U )

choose the missing hash function -> applying to the universe (all el in the universe) that function we will get a tuple that is not present in the remaini set of function  

how many bits -> m^|u| -> |u| log2 M -> very big  

in conflict with small space 

limiting randomness of hash -> not completly uniform -> full indipendence between real size of inputs not all > randomess in at most k el

fix k -> max k for with the vector k is compl uniform 

k=2 pairwise indep hashing

ax + b mod M where M is power of prim number -> pairwise ind

for any distinct x1 x2 -> input 
P(h(x1) = y1 \land h(x2)=y2 )=M^-2 -> proove this
apply h 

solve system as function of a and b  -> prob total -> just product of 2 prob 

number mod N of number is a field -> inverse exists -> call them z1 z2
prob that its some number 1/M

---
log2 M^k-1 

eval time O(k)

bot small if k small -> constant k is enugh and works 

---
universal hashing 
prob of having collision a little bit worse than k-wise
prob of collision  = 1/M

pairwise indipendent than also universal 

---
perfect hashing

we dont want collisions
on a particular hash function no collisions for any x, injective

given a set A before we can build perfect hash  function in constant time , in general not given the set, we cant guarantee it with prob = 1 but with high prob 1 - n^-c  c constant of choice 

---
perfect hashing via universal hashing

---
polynomial hashing

hash function on strings :
+ fix prime q 
+ choose z \[0,q) 
+ poly function is : 
character of the string are the coefficent of the string -> evaluate it in a random point 

propieties 

...

prob of collision = n / q 

non 0 pol of deg n -> at most n roots 
what is prob taht randomly we choose z as a root of the polynomial ( == 0 ) -> n/q

