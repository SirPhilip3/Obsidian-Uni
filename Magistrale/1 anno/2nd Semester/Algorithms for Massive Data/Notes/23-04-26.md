bloom filters 

filter -> randomized data structure 
$m$ = cardinality 
$n$ universe size

operations :
+ insert
+ membership -> fail with some probability $\delta$
+ sometimes delete

some filters have False Negative bloom does not 

$O(m \log 1/\delta)$
1/delta not always a constant 

used as interface to a slower and exact data structure

ask the bloom if exists if exist search in the disk otherwise no false neg gg -> good where many negative queries

filter generally good to fit in ram very small than in cache even better 

partition bloom filters actually better :
+ insert 
+ membership -> small prob has FP $\delta$
+ no delete count filters -> count ass with element -1 whne = 0 than delete

maximum capacity > fixed at the start

fully-uniform hash funcitons 

---
example , universe integers, any universe m cardinality 
M = 15 , k=3 

bloom filter -> bitvectro of l = M initially 0
partition in k part -> k iid hash function -> codomain the pos in the ith part 

if iid we would need the "mapping table"

insert
iterate 3 hash -> eval to 1 the bits corresponding
mem -> do same if all 1 = ok 

ex 4 fp , no in set but true mem -> happends bc of collision 

larger k is lower prob of failure 

---
k running time of the query -> iterate all hash funciton s

