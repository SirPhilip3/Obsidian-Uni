burrows wheeler transform 

permutation of a string 
if we can do rank select -> we can count in the original string
more compressible string

compute all string rotations 
first char at the end .... , 
one rotation for each row
sort them lexicographically

last column is borrows  wheeler transform

from right to left less clusterable -> first column we most compressible but we cant reconstruct the original string

character sorted lexicographiccaly based on suffixes ???? 

all charcter precede a given prefix -> high order entropy nH_k 

SA over bwt H_k while on the orignals string nH_0

sorting the suffixes lexicographically up to the $ 

order of the rotation -> the suffix array 
hp that S[0] = S[n]
i-th character in BWT is the i-1 in the SA -> we can build bwt from SA and oringal text 

insead of storing SA -> we store the char in the BWT (same with CSA ???? )

```
BANANA$
ANANA$B
NANA$BA
ANA$BAN
NA$BANA
A$BANAN
$BANANA
```

```
$BANANA
A$BANAN
ANA$BAN
ANANA$B
BANANA$
NA$BANA
NANA$BA


ANNB$AA
```

building linear time ? 

**LF** property 
*F*irst col, *L*ast col 

dont store the fisrt -> sorted last

realtion between char in L and F 
the i-th occ of a char in L is the same text postion of the i-th occ of the same char in F

ordered by prefix and circular so -1 is the start of the prefix 

sorting the p in L based on what comes after -> circular text waht follows is the start of the string 

pi -> first char go to L and jump to first i in F -> if we iterate for integer -> we get the original text muving backwards

inverse of LF -> FL moves forwards -> Psi function  

welback tree -> psi -> rank pos 3 of i F and than select i of 2 L
for F just store staring position -> binary search 

```
nb$aoob

$   n 
a   b
b   $
b   a  
n   o
o   o
o   b

baboon
```

compression boosting

WT(BWT(S)) nH_k o(n log sigma) 

sigma = alphabet size 

---
bad upperbound if k is large 
k only in the analysis -> wt we dont need the value of k 

psi here is log sigma time -> count with binary seach -> slower in respect to csa
better way with backwards search 

---
exercise 

online bwt

ANNB\$AA

```
$ABANANA
A$ABANAN
ABANANA$
ANA$ABAN
ANANA$AB
BANANA$A
NA$ABANA
NANA$ABA
```

an\$nbaaa -> edit distance = 2
we can do it dynamically by prepending chars 

add A after \$ dont change order + add the rotation with the dollar at the end , use lf mapping 

1 convert \$ into A
2 insert A and do rank_A on \$ -> new \$ must be the the result + 1  ???????