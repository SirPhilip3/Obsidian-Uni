information theory

some strings are more compressible than others 

strings of lenght n -> alphabet A C G T , everything is T except 1 A C G 

n pos for A , n-1 pos fro C n-2 for G -> n(n-1)(n-2) -> C(n 3)3!
it lowerbound log 2 (c(n 3)3!) -> O(log n) bits > very repetitive we have lots of T

position of A C G 

worst case entropy / info th lowerbound
---

$H = \log_2(|V|)$

any decodable encoding must use at least of lenght H for the longhest encoding otherwise we get collisions

we want to achive H for all the elements + small amount of extra space o(H)

$B_{n,m}$ -> all bit vectors of lenght n with exactly m bits = 1 

worst case -> log_2 of c(n m) -> m \log(n/m) + O(m) -> wort case also for set of integers 
1:1 between bitvectors and sets of integers 

ordinal tree -> tree where order of children matters 

total number of ordinal trees with n nodes -> 
worst case -> 2n - theta(log n)

storing pointers n log n bits 

upper bound -> with just 2n bits (()()()) nodes * 2 -> every node ( and ) > we can use bits > optimal encoding

empirical entropy
---
shannon entropy but on finite strings
zero order empirical entropy -> H0(S) = $\sum_{c \in \epsilon}\frac{n_c}{n} \log_2(\frac{n}{n_c})$

n_c -> frequency of the character c 
n -> lenght of string

$[\sigma]^n$ number from 1 to sigma 

set of coniantorial object with frequency of the character -> 12122132 $\in S(3,4,1)$ 3 of 1 , 4 of 2 and 1 of 3 

worst case of entropy -> 
$$\binom{n}{n_1}\binom{n-n1}{n2}\binom{n-n1-n2}{n3} = \frac{n!}{n1! \cdot n2! \dots}$$
multinomial coefficent

worst case is the log -> stirling approx -> $n\cdot H_0 - O(\sigma log n)$ H_0 for each character avg number of bits 

upper bounds 

intuition -> freq char with few bits -> no code must be prefix of another code 
huffman encoding

exploiting cotext 
---

programming book we have "ompile" -> the prev char will be c -> assign few bits to ancoding of c 

HIGH ORDER EMPIRICAL ENTROPY

$k\le n$ -> k = context lenght -> 6 for compile -> constant fro the whole compressor
$w$ context 

$S_w$ -> charcter that preced the context $w$ > concatenate toghether
S = *a*a*b*ab , k = 2 
S_ab = ab

circular if we have occurent at the start we thake the last as the next 

do for all sub string of k=2 ab, ba,aa,bb

than apply H0 to each Sw 
k order emphirical entropy -> weighted average of H0

entropy compressor
---

achieve n Hk + o(n log sigma) -> small o negligiable in some cases other will be bigger than n Hk

increasing contex always better compression 
why not use k = n -> no bc we must always have a term sigma^k -> frequencies for evry possible context 

largest k -> log_sigma n 

h1 -> 1st order emp -> 0 >

ex 3 -> 2nc/2n == nc/n
cannot compress repetitive strings 

last n * 2^n * n^(n \times sigma)

size of codomain ^ size of domain -> transistion fucntion DFA

beyond entropy
---

is entropy good measure of compressibility -> not always 
best id kolmogorov complexity -> lenght of the shortest program tha generets that string -> not computable 

no compressor can be optimal 

**worst case entropy applies to the wors case in set of object not on every object** -> some may be more compressible than other 

entropy fails -> Hk cant capture repetition longher than k

S = 01010101010101010101 , n = 16 -> not by 0 empirical entropy 
H1 -> 0 -> bc 1 always prec by 0 and same as before 
H1 can bee fooled -> 0110011001100110 -> half the times prec by 0 and half by 1 

just do entropy + number of repetition

repetitive string most of data is repetitive 

stronger compressor that captures repetition lempel 77, burros wheeler transform