how to measure space
+ words 64 bits at least log n bits -> to store at least a pointer , integer fp 
+ bits

avl tree> O(n) words -> O(n log n) bits 

avoid big O terms in space analysis

**full text indexing** 

definition : 
	given a text / string sigma^n -> lenght m over alpahabet sigam , build a data structure to do  queries :
+ locate P lenght m <= n (pattern) wheere it appear in T exact pattern matching  
+ count P -> ret |locate(P)| = occ occurencies
+ extract(i, L) -> return substring starting from i of lenght L-> in plain trivial 

solution : 
1. full text kept no data structure -> just scanning -> not efficeint time not proportional to the query 

time at least read the input and give the output 

locate -> at least m + occ  -> O(m + occ)
count -> at least m + 1 (one integer as output) -> O(m)
extract -> O(l) 

space -> quadratic space for trivial -> close to information theorietical lowerbound -> how many bit to express T -> log 2 (sigma^n) -> n log sigma bits  

if T very compressible much less

problem of buildign the index -> if i use cubic space useless 

in massive data no T in memory -> use stream 

T arrives on the fly and build it on the fly -> never storing T 

after the text T queries arrive as a stream -> when P1 finishes arriving i output the res 
at time we should not use more than the bit size of compresse T

how to contruct compressed F(T) on the fly -> *project*

ITL for human genome 3.2 bill  * log  -> 760 mb
compressability when we put togheter -> 1000 genome can fit in 1.5gb

SUFFIX TRee
---
quadratic spacec solution 

+ any substrin is a prefix of a suffix 

*ab*a*ab*
first is the prefix of the whole string
second is the suffix 

insert all suffixes of S put them in a tree and navigate it from the root 

alfabetical order 

node with at least 2 childre ordered from left ot right alphabetucal on the levels 

![[Drawing 2026-03-05 16.15.55.excalidraw.png]]
%%[[Drawing 2026-03-05 16.15.55.excalidraw.md|🖋 Edit in Excalidraw]]%%

at the end -> where there is a dollar put the starting index of the suffix 

link all leaves with pointer to the next left to right leaf 
every node pointer to the leftmost and eightmost leafes
every node number of leaves under the node 

asyntotic space is the same 

count = number of a -> number of suffixes that start with a so i walk to a and return the number stored there 

locate = find locus of patter jump to leftmost and than jump to the rightmost  from the statring position of the jumping + number of leaf says number of occurances of that pattern 

time of count O(m) -> walk m edges lenght of pattern -> optimal
locate  O(m + occ) -> walk m edges and than we walk occ pointers 

space -> worst is when : fist sufix 1 than 2 thane 3  === n*(n+1)/2 -> n^2  

reduce to n 

unary path only 1 child every node -> compress with just the starting position and ending -> label \[3,6] 

for all unary paths put any occurance of that -> b \[2,2] or \[5,5]

suffix tree result 

size -> every sufix is a leaf -> every node has at least 2 childre -> internal node at most n-1 -> number of leaves n -> n + n-1 = O(n)

we could end up in the middle of an edge -> this means that all occurences of aa are also occurences of aab 

same running time as before 

can be built online

suffix tree 8n words of 32 bits -> 95 gb

to n words
---

suffix array

before the suffixes sorted alphabetically \$ is the previous one

we can remove the tree and use only the suffix array + text -> binary search
jump in the middle extract text decide if smaller or larger and jump either left or right to find the end of the cluster

at every step we need to extract at most m char -> query lenght 
log n each time extract at most m -> m log n time

space m word + text 

SA + text n log n + n log |E|

locate m log n + occ -> when we find the range enumerate for all the number inside the range 

extract O(l) -> bc we are storing the text -> just jump in the text

the text fits in n log |E| (information theo opt) + o(n log |E| very small ) even less if compressible

Exercise
--- 

