bit-vectros datastructures 

supporting access , rank and select queries 

access -> input position i and ith bit result -> *contant* time naively  
rank -> B.rank_0(i) -> number of 0 in the first i bits same for \_1 rank is including 
select -> B.select_0(i) -> poistion of the ith 0 same for 1 

bitvector and set same thing 

count locate and extract -> will we map it to access rank and select 

memberchip -> of x in S -> check if B\[x] = 1
predecessor of x -> largest y<=x in the set -> get the previous 1 to the one in B[x] -> 
select_1(rank_1(x)) rank will tell you the lenght of 1s -> last one = lenght -> accessing that one is the closer to the el

---
if sparse dataset binomial coefficent very small so the o(n) will dominate bad on sparse and dense set

ex store 1000 ip but number of if 2\^32 -> very sparse

model of computation ---> as close as possible to our pc -> *word RAM* model 

words of w = \theta(log n) bits -> n is the imput size 
constant time bitwise op 

shift right -> sub

bit packing -> bool array -> divide by 8 bits the size 
n/w blocks -> each one is an integer -> bc normally it will store for each block 8bits for bool

ho to extract pos / word size > than shift 

1001 0110 -> extract the 6th bit i=6
v = 9 6

i/w -> to see what integer -> so 7 > modulo discover the position -> 2nd one 
extract second bit from 0110 
x >> 1 -> w - the position -> 0011 -> get rid -> *and* 0001 = 0001

we can also get any subseq of l max 1 word 

---
reduce space to nH0 

compression in nH0 + o(n) bits
intuition 

fix a block size -> b = log_2 n / 2 -> works fro any constant larger than 1 
divide bitvector in block of size b
build table containing all possible bitvectors of lenght b

T\[k] -> all possible bitvectrof of lenght b with k 1s
we can encode our bitvectro as pointer to this table -> variable lenght

table o(n) -> since the bitvectros are small 

example 
n = 21 b = log2 21 / 2 -> 3
```
010 101 001 110 000 011 111 
```

class of a block -> number of 1s in the block
C = 1 2 1 2 0 2 3
```
   010 101 001 110 000 011 111 
C = 1   2   1   2   0   2   3 -> row of the table
```

size of C n/b -> large b each -> log b+1 -> O(log log n)
b = O(log n)

sort them in T -> smallest to largest -> T all possible blocks even if not present in the bitvector

table space -> every cell logn bits , b+1 rows 
cols = at most 2\^b 
tot = 2^b * b^2 -> with b as log2 n / 2 -> O(logn^2) * n^(1/2) -> o(n)

O -> the column where the block is present in T

T can be recontructed from the size of the blocks

bits to encode the offsets log c(b k) -> cardinality of the set 

---
$$
\binom{x}{y} \cdot \binom{w}{z} \leq \binom{x+w}{y+z}
$$
bc it grows exponentially the frist by just x\*y the second x^y 
proof
$$
\sum^{n/b}_{j=1} \log\binom{b}{c[j]} = \log \prod^{n/b}_{j=1} \binom{b}{c[j]}\leq \log\binom{n}{m}\leq n H_0
$$
m = number of 1s in all blocks c\[j] sum of 1 of each blocks

----
exercise

log2 70 / 2 = 3.06 = ceiling 4

T table

|     | 0    | 1    | 2    | 3    | 4    | 5    |
| --- | ---- | ---- | ---- | ---- | ---- | ---- |
| 0   | 0000 |      |      |      |      |      |
| 1   | 0001 | 0010 | 0100 | 1000 |      |      |
| 2   | 0011 | 0101 | 0110 | 1001 | 1010 | 1100 |
| 3   | 0111 | 1011 | 1101 | 1110 |      |      |
| 4   | 1111 |      |      |      |      |      |
0 2 3 2 0 -> bits for row from top to bottom

```
  0110 1000 0000 1111 0101 1011
C   2     1   0     4    2   3
O   2     3   0     0    1   1
eO  010   11            001  01  -> encoding
```

than encode in biniary and put C + O
O -> 3 bits to point to -> 6 disct values 
