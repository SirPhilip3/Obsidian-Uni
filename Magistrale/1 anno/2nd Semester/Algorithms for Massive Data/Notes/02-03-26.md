prezza 

**exam -> project + oral 50 50**

**project -> massive data** problem sol with tecniques learned during course 
-> nice problem no naiveness 

take exam whenever 2 weeks for oral exam

avl trees
topics
---

focus on space usage for data structures 

compress the data and data structures 

compress data does not allow operation on data -> compressing data structures maintains this 
adl trees 

traditional store everything -> build data structure on it -> than query on that 

data arrive in streams -> cant be stored -> compress data + data structure and original data trash 
soutions
+ lossless compression -> no info losable -> only works if data has redundancy
+ lossy data strucutre -> we lose some data -> sketching -> exponential compression 

wget -o | grep -i 

sketching -> randomized algo -> approximate answers -> wrong with some probabilty 

data types
---

+ set of integers 
+ strings
+ vectors $\mathbb{R}^n$ -> distance etcc 
+ relational algebra SQL -> number result approximated result (project)

collection of m integers  -> univers number from 1 to n -> $[n]$
m choose n possible sets 

binary code to encode m bits -> we cant use less than $$ \log_2{n \choose m}$$ bits -> longhest code can not be shorter than this -> ran out of combiantion  of bits for each encoding 

expanded -> $m \log(n/m)+ O(m)$ bits -> slightly less -> integer could be correlated -> sorted -> every integer selected depends on the prev -> we can store the difference instead of the full numbers  -> integers smaller than before 

solving queries mybe difficult

if we dont want to lose info 

steeling approximation 

**Information Theoretic lower bound** -> entropy from information theory
for set of integers

| space                      | queries supported                                                                                            | note                                                                   |                |
| -------------------------- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------- | -------------- |
| log2 c(m n) + O(m)         | membership , pred , succ                                                                                     | lossless , loga time queries                                           | Elias fano     |
| log2 c(m n) + o(m)         | memebrshib , pred , succ                                                                                     | lossless contant time                                                  | RRR bitvectors |
| O(m)                       | insert membership                                                                                            | lossy , queries may fail with some prob                                | filters        |
| O(poly(n+m)) => log(m+n)^c | insert, cardinality, no membership bc refequency has higher deviation than 1, frequency estimation / moments | lossy , queries result are approximate + may fail with very small prob | sketches       |

strings -> pattern matching problem -> given text > membership of some strings even without bounds

asym notation
---

exercises examples : 
+ theoretic lower {a,b,c,d}^n -> max lenght of n 4^n possiblieties -> \log_2 4^n -> 2n -> counting how many combiantion of objects 
+  same as before but always 2 a in the string 3^n-2 \* c(n 2) -> where i put the 2 a -> n-2 \log_2 3 + \log c(n 2) = n-2 \log_2 3 +\log O(n^2) -> n log 3 + O(log n) -> entropy compression 
4 -> yes 
